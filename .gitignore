# Secrets: never commit these
.env
*.key

# Local databases and bulk data downloads
*.sqlite
*.sqlite3
data/

# Python
__pycache__/
*.pyc
.venv/
*.egg-info/
.pytest_cache/

# Local demo material and scratch files (never for the public repo)
demo/
prep_body.txt

# macOS
.DS_Store

# Claude Code session config (local tooling, never public)
.claude/
