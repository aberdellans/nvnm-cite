# Decisions

Append-only. One dated entry per settled choice, newest at the bottom. Open questions do not live here; they live as unchecked tasks in IMPLEMENTATION_PLAN.md.

## 2026-06-10: Chain constants corrected from the v1 draft plan
The draft (docs/ARCHIVE/build-plan-v1-2026-06-09.md) targeted testnet chain ID 58887. That chain (manveniam-1) is retired. Live testnet: nvnm-testnet-1, EVM chain ID 787111, RPC https://evm.testnet.nvnmchain.io, Blockscout explorer https://explorer.evm.testnet.nvnmchain.io. The draft's mainnet explorer URL (explorer.nvnmchain.io) is the Cosmos-side explorer and cannot display EVM transactions; the EVM explorer is https://evm.explorer.nvnmchain.io. Sources: NVNM_MCP_Server/CLAUDE.md chain-ID history table, nvnm-tutorial README.

## 2026-06-10: Precompile semantics adopted from NVNM_MCP_Server; discovery phase narrowed to experiments
Known from the sibling project (ABI vendored at abi/anchoring.json): addRecord takes a 10-field tuple of plain strings and the module does no content validation, so plaintext citations fit in the checksum field natively; records(registry, checksum, recordId, index, pagination) supports keyed reads by registry name + checksum string; the registry creator becomes its admin; grantRole(registryId, checksum, account, role) grants admin/editor at record, registry, or global scope; the precompile emits no events. The genuinely unknown behaviors are Phase 0 experiments (a) through (i) in IMPLEMENTATION_PLAN.md.

## 2026-06-10: Lookup design: keyed chain read AND local index (the draft's options A and B, both)
Receipt-time checks use the direct records() eth_call so a receipt can claim a chain read at a stated block height. Drafting-time checks use the local SQLite index only (no RPC, no trace). The index is rebuildable from chain by anyone with an RPC URL (rebuild-index command), which is the public-auditability claim made executable.

## 2026-06-10: Signer written from scratch in pure Python (user decision)
The draft said to reuse evm_signer_v5.py; that file does not exist on this machine. Albert chose a from-scratch pure-Python signer over the eth-account library: keccak-256 (original Keccak padding; hashlib.sha3_256 is NIST SHA-3 and gives different digests), RLP, secp256k1 with RFC-6979 deterministic k, EIP-155 type-0 signing. chain/ stays stdlib-only. Golden vectors: Keccak team KATs (pre-NIST, original padding), RFC 6979 Appendix A, the EIP-155 worked example (chain id 1, nonce 9, v=37), ethereum/tests RLP cases, plus cross-check vectors generated locally with ethers (node v20 installed). Known tradeoff, accepted: hand-rolled crypto is normally a liability; acceptable here because session keys are throwaway testnet keys, RFC-6979 removes RNG misuse risk, and mainnet writes happen outside Claude Code sessions per the pilot-close preconditions.

## 2026-06-10: Transaction type: legacy type-0 only for V1
The chain accepts EIP-1559 type-2 transactions, but at a fixed 40 gwei floor the typed envelope adds complexity for zero functional gain. Revisit only if Phase 0 shows type-0 rejected.

## 2026-06-10: Pilot corpus: SCOTUS + 11th Circuit
Chosen so the flagship demo's fabricated cite (Varghese v. China Southern Airlines, 925 F.3d 1339 (11th Cir. 2019), from Mata v. Avianca) maps to a covered registry and returns NOT_FOUND from a chain read instead of "court not covered". Gate: the Phase 2 census must confirm CourtListener ca11 coverage (the coverage API reported ~95k opinions, 1981 onward, fetched 2026-06-10). Fallback: 2d Circuit, with a demo cite swap.

## 2026-06-10: SCOTUS corpus is roughly 8x the draft's assumption; load in tranches
The CourtListener coverage API reports ~522k scotus opinions (post Caselaw Access Project merge; cert denials, GVRs, and memoranda are each their own citable cluster). For an existence registry these are legitimate entries: a brief citing a cert denial cites a real citation. Tranche 1 = ca11 complete + SCOTUS precedential; tranche 2 = SCOTUS memoranda, gated on the Phase 0 experiment (i) budget check and the Phase 2 census numbers.

## 2026-06-10: Record schema draft (locks at Phase 1 task 1.5)
Per-case record: checksum = canonical citation plaintext, checksumAlgo = "cite-canonical-v1", uri = CourtListener cluster URL, metadata = compact JSON {name, year, cluster}. Receipts (registry receipts-v1): checksum = document SHA-256, checksumAlgo = "sha256", metadata = full receipt JSON. Registry names = courts-db IDs prefixed "us-". Canonical citation key = the reporters-db EDITION string (F.3d, not F.) with the FIRST-PAGE rule: interior/pin pages are never keys. The first-page rule is load-bearing: the fake Varghese cite (925 F.3d 1339) falls on an interior page of a real case (J.D. v. Azar, 925 F.3d 1291, D.C. Cir.), and first-page keying is why it still returns NOT_FOUND.

## 2026-06-10: Status enum: five statuses plus an orthogonal name_check field
VERIFIED / NOT_FOUND / NOT_COVERED / AMBIGUOUS_JURISDICTION / UNPARSEABLE. NOT_COVERED is new versus the draft: a cite that parses and maps to a court with no on-chain registry (for example a ca2 cite during the scotus+ca11 pilot). Without it, out-of-corpus cites would falsely smell fabricated, and receipts would over-claim. Per-result field name_check: match | mismatch | unknown, a fuzzy compare of the brief's party names against the registry record's case name. It catches the other Mata failure mode (a real citation paired with an invented case name) while staying inside the existence-only invariant: it reports what the registry metadata says the citation denotes, nothing about holdings. It is a field, not a status, because existence and name-pairing are different dimensions and name matching is heuristic.

## 2026-06-10: Golden vector sourcing: published constants + ethers as the independent oracle
Tasks 0.1/0.2 pin keccak-256 and RLP with two independent sources: digests and encodings published in the literature and the Ethereum specs (hardcoded in the tests), and vectors generated by ethers 6.16.0 (shares no code with this repo), vendored with their generator scripts under tests/golden/. This replaces the originally planned download of the Keccak team's KAT files: same independence property, no network dependency, and the generator scripts make regeneration reproducible. The EIP-155 worked example and RFC 6979 Appendix A vectors still come from their primary documents in tasks 0.3/0.4.

## 2026-06-10: Task 0.6 live-testnet findings (round-trip succeeded; dev-probe registry id 733)
- The public RPC sits behind Cloudflare, which blocks the default Python urllib user agent (error 1010); rpc.py identifies as `nvnm-cite/0.1.0`.
- Keyed query misses ERROR instead of returning empty pages: `registries(name=<missing>)` and `records(<registry>, <absent checksum>)` both raise `collections: not found: key ...` through eth_call. The verifier's NOT_FOUND path and the registry-existence check must catch this RpcError pattern; an empty page is NOT the miss signal. A keyed hit returns the full record.
- Chain-set fields: recordId and index number from 1, not 0. Timestamps come back as Go time strings (`2026-06-10 23:10:26.453704492 +0000 UTC`). Registry `creator` is returned in bech32 (`nvnm1...`), not 0x form, even via the EVM interface.
- `count_total=True` on a keyed single-record query returned `total=0`; treat the total as meaningful only for list/pagination queries pending the (g) experiment.
- Gas, measured: addRegistry 63,587; addRecord with ~120 bytes of strings 83,201; node-suggested gas price 45 gwei; eth_estimateGas was exact to the unit on both. Record cost ≈ 0.0037 wmantraUSD (~$0.004). Extrapolation: tranche-1 bulk load (~300k records) ≈ 1.1-1.5k wmantraUSD. The wallet's 10 wmantraUSD covers all Phase 0 work but NOT the Phase 2 bulk load; arrange funding before the load session.
- The from-scratch signer is chain-validated end to end: the node's computed tx hash matched ours on both sends.

## 2026-06-10: Task 0.7 experiment matrix results (Phase 0 close; testnet registry dev-probe id 733)
Raw runs: scripts/phase0_experiments.py (quick / throughput / post) plus targeted follow-up probes; ~0.97 wmantraUSD total Phase 0 spend; all measured on nvnm-testnet-1 (787111). Conclusions were adversarially audited against the raw outputs before this entry was written.

(a) **Duplicates VERSION, never reject.** Re-anchoring the same (registry, checksum) succeeds, keeps recordId, bumps index (observed to index=4); isLatest moves to the newest; keyed reads and list views return only the latest version. eth_estimateGas does NOT reject duplicates, so there is no free idempotency probe. SUPERSEDES the bootstrap entry's "one local source says the precompile rejects duplicates (mainnet experience, May 2026)". Loader consequence (task 2.4 amended): the checkpoint DB is the only idempotency guard; never blind-resubmit.

(b) **String caps and required fields, measured.** metadata max 2048 B, uri max 2048 B, checksum max 64 B (each rejection names its limit). uri, checksumAlgo, and metadata are all REQUIRED non-empty, and metadata "{}" is rejected as empty (an empty JSON object counts as empty). SUPERSEDES the bootstrap entry's "the module does no content validation". Gas below the caps: ~76 gas per metadata byte + ~77k fixed (256 B -> 96,219; 1,024 B -> 154,692; 1,024 B confirmed on chain and read back intact). Plaintext multi-word checksums with spaces and periods are accepted, so the citation-in-checksum design stands; canonical citations fit the 64 B cap with huge margin, and a bare sha256 HEX digest fits it exactly (64 chars, zero room for suffixes; base64 form is 44 chars if suffixed keys are ever needed). CONSEQUENCE: a full receipt JSON cannot live in one record's metadata; the chunked/compact receipt design is now required, not a fallback (tasks 4.2/4.3 amended).

(c) **Registry names are UNIQUE**: duplicate addRegistry rejected at estimation ("registry already exists with this name"). Name-keyed lookups are unambiguous and the estimate-probe is a free idempotency check for registry creation.

(d) **Write auth is deny-by-default; grantRole works.** A never-granted address gets "unauthorized"; after grantRole(733, addr, "editor") (41,775 gas, confirmed on chain) the identical calldata estimates cleanly from that address. Deny/allow evidence is at the eth_estimateGas simulation level (unfunded throwaway keys); the Phase 2 multi-key load will exercise it with funded sends. The third-party-attestor invariant is validated, and multi-key parallel loading is possible.

(e) **Single-sender throughput plateaus at ~1.1 tx/s**: 0.29 / 0.73 / 1.11 / 1.13 tx/s at pipeline depths 1 / 5 / 20 / 50 with contiguous nonces (no mempool pushback). Nonce-GAPPED submission is REJECTED outright ("tx nonce: 270, account accountNonce: 269"), so per-key submission must be strictly serialized in nonce order, a failed send halts everything queued behind it, and recovery means re-sending the same nonce. ~95k records per key per day. Phase 2 plans parallel editor-granted keys; whether the plateau is per-sender or global resolves during the multi-key load.

(f) **updateRecordStatus**: absent from the vendored ABI; nvnm-tutorial (May 2026) reported it missing from the deployed binary; not probeable without its authoritative signature. Correction policy: supersede-by-new-version (proven viable by (a)), with revocation expressed in the superseding version's metadata.

(g) **Pagination**: the server caps every page at exactly 200 rows (limit=300 and limit=10000 both returned 200 of the 264 present); nextKey is never populated; countTotal returned 0 on every query shape, which closes the 0.6 entry's "pending the (g) experiment" caveat: totals are unreliable everywhere and must be counted client-side. Full sync = OFFSET-based paging until a short page. List views show only the latest version per record. Re-verify the 200 cap at Phase 2 scale.

(h) **The public RPC is a full archive node**: balances served at block 1 and every depth tried, and a direct bidirectional observation at the registry's creation boundary (absent at block 1,573,400; present, id 733, at 1,573,510). Pinned-block receipt re-verification works on the default public endpoint; task 4.4's rebuild-to-height fallback stays documented as resilience against future pruning changes.

(i) **Budget decision**: Phase 0 spent ~0.97 wmantraUSD; ~9.0 remains, ample through Phases 1-3 including demo-scale anchoring. Tranche-1 bulk load at measured gas (realistic citation records ~96-155k gas each) ~= 1.2-1.6k wmantraUSD. DECISION: tranche 1 does NOT proceed on the current balance; before the Phase 2 load session, top up roughly 2,000 wmantraUSD via the DuKong bridge flow (nvnm-tutorial/README.md) and settle the multi-key loading strategy.

## 2026-06-10: Git strategy: main-only with phase-N-done tags
No feature branches: solo project, no reviewer, and the continuity protocol depends on every new session resuming from one unambiguous checkout (CLAUDE.md auto-load reads the working tree). Tags give milestone/rollback semantics read-only. Repo: github.com/aberdellans/nvnm-cite, private; can transfer to an org later if needed.

## 2026-06-10: Phase 1 dependencies: eyecite + reporters-db + courts-db (Free Law Project stack)
Added via uv (pyproject lower bounds, exact pins in uv.lock): eyecite 2.7.6 (BSD-2-Clause), reporters-db 3.2.65 (BSD-2-Clause), courts-db 0.10.27 (BSD-2-Clause). Transitive: fast-diff-match-patch 2.1.0 (Apache-2.0), lxml 6.1.1 (BSD-3-Clause), pyahocorasick 2.3.1 (BSD-3-Clause + public domain), regex 2026.5.9 (Apache-2.0 + CNRI-Python). All permissive; licenses read from installed package metadata, not assumed. These are runtime deps of the normalizer only; `chain/` stays stdlib-only per the Phase 0 convention.

## 2026-06-11: Normalizer behavior pinned against the eyecite stack (tasks 1.2/1.3)
Measured against eyecite 2.7.6 and compensated for in nvnm_cite.normalizer:
- resolve_citations silently DROPS unresolvable short forms (orphan Id./supra/short cites). The normalizer re-reports them as UNRESOLVED occurrences; a dangling Id. must stay visible to the verifier.
- Bare U.S. and S. Ct. cites get court='scotus' from eyecite; bare L. Ed./L. Ed. 2d do NOT (court=None). The us-scotus mapping therefore uses the explicit edition set {U.S., S. Ct., L. Ed., L. Ed. 2d}. All four carry reporters-db cite_type='federal', so cite_type cannot discriminate.
- reporters-db cite_type='scotus_early' is a grab bag (includes 'Bee', "Oliver's Forms") and 'Cranch' is also a D.C. reporter: bare early nominatives ("1 Cranch 137") return AMBIGUOUS_JURISDICTION per never-guess; "5 U.S. (1 Cranch) 137" maps to us-scotus via the U.S. edition.
- eyecite raises on empty input (guarded in normalize()); plaintiff/defendant can come back '' (normalized to None); metadata.year can bleed between citations in tangled text (treated as informational only, never part of keys).
- Normalizer-level dispositions: OK | AMBIGUOUS_JURISDICTION | UNRESOLVED, distinct from the verifier's five-status enum; AMBIGUOUS_JURISDICTION maps 1:1, UNRESOLVED covers occurrences with no canonical key (orphan short forms, pending-publication cites).

## 2026-06-11: cite-canonical/v1 spec + record schema v1 LOCKED (tasks 1.4/1.5; reviewed by Albert in-session)
docs/canonical-citation-spec.md and docs/record-schema.md are locked; changes now require version bumps (new checksumAlgo / schema strings), never edits. Settled in the lock, beyond the 2026-06-10 draft:
- Receipt uri = urn:nvnm-cite:receipt:v1 (Albert's pick over a GitHub URL): the repo is private and no public host exists, so a URL written today would dangle for third-party chain readers; a URN is an honest identifier. Move to a published URL at mainnet cutover under a version bump.
- Per-case uri = https://www.courtlistener.com/opinion/<cluster_id>/<slug>/ with the slug from bulk data; deterministic fallback https://www.courtlistener.com/api/rest/v4/clusters/<cluster_id>/ when the slug is missing. CL's bot protection (HTTP 202 challenge) blocked verifying arbitrary-slug redirects, so the schema depends only on data in hand.
- Registry creation strings fixed (descriptions + metadata JSON incl. Free Law Project attribution) so Phase 2 never improvises them.
- Metadata cap handling: deterministic truncation (name ellipsis at UTF-8 boundary; collision form caps names at 256 B each, then drops trailing entries adding "omitted":N). Oversize uri halts the writer; never silent truncation.
- JSON serialization everywhere: UTF-8, ensure_ascii=false, sorted keys, no whitespace; byte budgets measured on UTF-8.
- Submit-side tuple: timestamp="", recordId=0, index=0, isLatest=false; chain values authoritative on read.

## 2026-06-11: eyecite gap: "(3d Cir.)" unrecognized; closed-set circuit fallback added (same-session spec clarification)
Measured: eyecite 2.7.6 resolves "(3rd Cir.)" to ca3 but returns court=None for the Bluebook-standard "(3d Cir.)" (while "(2d Cir.)" works; the gap is the Third Circuit ordinal only). Briefs overwhelmingly use the Bluebook form, so pinning AMBIGUOUS_JURISDICTION there would have been wrong for the single most common ca3 spelling. Resolution: jurisdiction.py applies an exact-whitelist fallback when eyecite reports no court: the 13 standard federal appellate parenthetical forms (1st-11th incl. 2d/3d ordinal spellings, D.C., Fed., each followed by "Cir.") matched immediately after the citation span, with only pin-cite characters permitted in between so a neighboring citation's parenthetical can never be misattributed (regression test pinned). Spec section 4 rule 2 was clarified the same session, before any chain record exists under cite-canonical/v1, with Albert's lock done in-chat; this is a pre-publication clarification, not a post-lock edit. Consider an upstream eyecite report post-pilot.

## 2026-06-11: Task 1.6 fixtures: RECAP provenance + the scanned-affirmation finding + pdfplumber (dev)
- Golden suite: 214 vectors / 230 expectation rows in tests/golden/normalizer/vectors.json, generated by generate_vectors.py from spec-derived static tables (the generator never imports nvnm_cite, preserving independence). Contract fields compared exactly: as_written, canonical, registry, disposition, kind. A suite-size test enforces 200+.
- RECAP integration fixtures (tests/fixtures/briefs/, manifest.json with source URLs and sha256s, retrieved 2026-06-11 from storage.courtlistener.com): Mata v. Avianca ECF 21 (the ChatGPT affirmation), ECF 24 (Avianca reply memo), ECF 54 (sanctions opinion), and Walters v. OpenAI ca11 appellant brief ECF 9 (contains a real parallel-citation triplet in tight no-space forms, 494 U.S. 472 / 110 S.Ct. 1249 / 108 L.Ed.2d 400, all normalized correctly).
- FINDING: the infamous Mata ECF 21 affirmation is an IMAGE-ONLY scan; its text layer is just ECF header stamps (~62 chars/page), so extraction honestly yields zero citations (pinned by test). It stays as the Phase 3 extraction-recall fixture; the demo keeps using a born-digital fixture brief per task 6.1.
- Extracted .txt files are committed; integration tests need no PDF tooling at run time. pdfplumber added as a DEV dependency (regeneration + Phase 3 runtime candidate): pdfplumber 0.11.9 (MIT), pdfminer-six 20251230 (MIT), pillow 12.2.0 (MIT-CMU), pypdfium2 5.9.0 (BSD-3-Clause/Apache-2.0), pycparser 3.0 (BSD-3-Clause). Licenses read from installed metadata.
- Live-document results: ECF 24 finds Varghese (925 F.3d 1339 -> us-ca11) among 31 occurrences; ECF 54 yields 97 occurrences incl. Varghese twice; Walters resolves to exactly {us-ca11, us-scotus}.

## 2026-06-11: Phase 2 bulk-load key strategy: SINGLE key (Albert's decision)
No worker keys, no grantRole for the pilot load: the funded key (0xaf639Dc7632eD8BE9718458EF74Ded3204bA7EcB) creates the registries and writes every record. Rationale: a multi-day sustained single-sender run is itself a useful stress test of the chain, and one nonce lane keeps operations simple. Accepted cost at decision time: ~3-day wall clock at the then-believed ~1.1 tx/s plateau (superseded the same day, see the load entry below: the plateau was client-side). Deferred with this decision: the multi-key fleet design, the per-sender-vs-global throughput question as originally framed, and the funded-send validation of grantRole from experiment (d) (still simulation-level only). All of it moves to mainnet load planning.

## 2026-06-11: CourtListener bulk snapshot 2026-03-31; the COPY dialect is backslash-escaped (silent-desync hazard)
Snapshot: latest available (citations 127 MB / opinion-clusters 2.45 GB / dockets 4.98 GB bz2, from storage.courtlistener.com/bulk-data/). MEASURED dialect: unquoted header row; every non-NULL value double-quoted (FORCE_QUOTE *); embedded quotes BACKSLASH-escaped (PostgreSQL ESCAPE '\', NOT csv-style doubling); literal newlines inside quoted values; NULL = bare empty field. Python's default csv dialect does not error on this: it silently DESYNCS at the first embedded quote and shreds subsequent rows (first run "lost" 312 scotus/ca11 dockets and crashed mid-clusters on garbage). Correct reader: csv.reader(f, escapechar="\\", doublequote=False), verified by a uniform 36-field parse over 200k consecutive cluster rows with embedded quotes decoding correctly. The loader also skips-and-counts structurally short rows into corpus meta (malformed_rows) so real dump corruption stays visible. Clean-run results: 70.0M docket rows -> 679,223 kept (scotus+ca11); 589,930 clusters; 1,379,162 citation rows; 0 malformed; 20.6 min wall.

## 2026-06-11: Census results (task 2.2); tranches are reporter-whitelists, not precedential_status
Per-court: ca11 90,459 clusters (54,129 Published / 36,330 Unpublished), 133,190 keyed citation rows, 100,726 distinct keys; scotus 499,471 clusters, 1,245,972 keyed rows, 411,227 distinct keys. KEY FINDING: scotus precedential_status is 498,934/499,471 'Published' (cert denials and GVR memoranda included), so the planned precedential-vs-memoranda tranche split CANNOT be expressed in CL data. Tranche 1 is therefore defined by the verifier-reachable reporter whitelist (exact CL reporter strings, which are reporters-db editions): us-scotus {U.S., S. Ct., L. Ed., L. Ed. 2d}, us-ca11 {F.2d, F.3d, F.4th, F. App'x}. Excluded and counted, loadable as a future tranche if ever wanted: early nominatives (Wall./How./Pet./Cranch/Dall./Black/Otto -- bare nominative cites are AMBIGUOUS_JURISDICTION per spec, their U.S.-series parallels are loaded), U.S.L.W., WL/LEXIS vendor cites (NOT_COVERED during the pilot per the known-risks list), BNA/CCH specialty reporters, and non-edition variant spellings found raw in CL ('Fed. Appx.' x21, curly-apostrophe 'F. App'x' x10). Load set: 260,763 records = us-scotus 218,775 + us-ca11 41,988. Collisions are structural, not exceptional: 124,918 scotus + 6,713 ca11 keys denote >1 cluster (order-list pages; worst key 111 F.3d 897 spans 49 clusters; metadata collision form with "omitted" truncation engaged, max metadata exactly 2048 B). DEMO GATES PASSED: '925 F.3d 1339' absent (0 rows); ca11 2019 F.3d coverage = 246 clusters. Schema v1 clarifications settled pre-load (deterministic, recorded here per the no-edits rule): metadata 'year' key is OMITTED when the cluster has no date_filed (0 such clusters in tranche 1); a collision record's uri is the LOWEST cluster id's URL.

## 2026-06-11: Court registries created on testnet
us-scotus = registry id 737 (tx 0x340bc336894b928a37a46569959bd3ecac7e2dd52f64f70a14e2a958a82807ce, block 1,593,850, gas 79,163); us-ca11 = registry id 738 (tx 0xdf7d76f6bbdc88a514f2667599a64ce4849a1b1339d57cc73159128683cadabf, block 1,593,853, gas 80,791). Creation strings rendered mechanically from the locked schema doc section 2 (courts-db names, FLP attribution in metadata) by scripts/phase2_create_registries.py; idempotent via the name-keyed registries() read.

## 2026-06-11: Tranche-1 load launched; optimized mid-flight; the "1.1 tx/s plateau" was OUR client, not the chain
Run 1 (per-record estimateGas + per-tx receipt polling, all serial un-pooled HTTP): stable ~0.76 tx/s marginal, 1,899 records confirmed, avg 97,545 gas/record, ~8.3 wmantraUSD spent, ETA ~4 days. Stopped via SIGTERM (clean drain + checkpoint), optimized with Albert's OK, resumed -- total downtime ~3 min, zero lost records (the checkpoint DB is the resume guarantee, exercised for real). Optimization: (1) gas limit from the measured curve (75,000 + 80/payload-byte, +30% headroom; phase-0 estimates were exact-to-the-unit at 256/1024 B and run-1's 97.5k average sits on the line; unused limit refunds, reconcile catches anything gas gets wrong) -- no estimateGas call per record; (2) confirmation by account-nonce advance (one eth_getTransactionCount settles the whole drained window) with receipts SAMPLED every 50th nonce for revert detection. Run 2: ~2.07 tx/s overall and still climbing (~2.2 marginal) with in-flight 1-6 of depth 50, i.e. the loader is SUBMISSION-bound (sign + send round-trip) and the chain absorbs >=2.2 tx/s from a single sender. CONSEQUENCE: experiment 0.7(e)'s "single-sender plateau ~1.1 tx/s" was the measuring client's per-tx overhead, not chain policy; the mainnet cost/time model must not cite 1.1 tx/s as a chain limit, and further client speedups (persistent HTTP connection, batched signing) are available if ever needed. ETA at stable rate ~35 h (~1.5 days). Operations: detached `nohup caffeinate -i ... bulk_load run` (PID in data/load.pid, log data/load_tranche1_run2.log; caffeinate stops idle sleep but NOT a closed lid -- machine stays open/powered); stop = SIGTERM (drains in-flight, checkpoints); resume = re-run `bulk_load run` (recovery waits out the mempool then settles 'submitted' rows via keyed reads); `bulk_load status --offline` for counters without RPC. Next session: indexer sync of both registries + reconcile (task 2.6 close-out), then 2.7. OPEN ITEM, flagged to Albert: an unexplained +300,000 wmantraUSD landed on the load key mid-session (3,109 -> 303,109 before the registry txs); confirm the bridge transfer was intentional before relying on that balance for anything.

## 2026-06-11: The +300k wmantraUSD top-up was intentional (Albert, same day)
The mid-session +300,000 wmantraUSD on the load key was a deliberate bulk top-up covering this and all future testnet projects, so funding stops being a per-phase chore. Closes the open item in the tranche-1 load entry above. Testnet token only; the mainnet-keys-never-in-sessions rule is unaffected.

## 2026-06-12: Web demo frontend built out-of-band (Albert's request; not a phase task)
`src/nvnm_cite/webapp/` + `docs/web-demo.md`: a stdlib HTTP server (port 8787) and a no-framework, no-CDN static page giving lawyers four flows: local citation check, wallet-signed receipt anchoring, free hash lookup for clerks/counterparties, and a calldata decoder (Blockscout's UTF-8 view renders ABI framing as mojibake; this decodes anchoring txs to labeled plaintext with the project codec). Settled choices:
- **No new runtime dependency, no JavaScript ABI code.** The server reuses the golden-tested `chain/abi.py`/`precompile.py` for every encode/decode; the browser signs server-prepared calldata via the user's wallet (page never sees keys; server never signs). pdfplumber (already a dev dep) is import-guarded for PDF extraction; absent it, the server asks for .docx/.txt. DOCX extraction is stdlib (zipfile + etree, document+footnotes+endnotes).
- **Drafting checks are RPC-free** (invariant 3 rationale now written down in docs/web-demo.md): `/api/check` reads `chain_index.sqlite` when the registry is synced, else `corpus.sqlite` restricted to the tranche-1 reporter whitelist (mirrors the load set; provenance labeled per registry in the report). Uploads processed in memory only — no disk, no logging of content, stateless server. Receipt preparation does the task-3.2 receipt path: keyed `records()` reads pinned to one block; transport errors never classify as NOT_FOUND. Statuses are the locked five-status enum; normalizer UNRESOLVED reports as UNPARSEABLE with reason; `name_check` is a conservative token-overlap heuristic (mismatch only when both parties present and nothing matches), a preview of task 3.3, not its implementation.
- **Receipts are marked DRAFT until 4.1 locks**: schema string `nvnm-cite-receipt/v1-draft`, shape per the 4.1 sketch, compact result entries per the 4.3 hints (keys c/w/g/s/n/k/o documented in docs/web-demo.md), deterministic overflow ladder for the 2048 B cap: drop `w` → collapse VERIFIED+match into `verified_omitted` (NOT_FOUND/mismatch entries never collapse) → refuse (chunking is 4.3). When 4.1 locks, the webapp flips to the locked builder; v1-draft testnet receipts are demo artifacts, distinguishable forever by their schema string.
- **receipts-v1 creation is offered in the UI** with the locked schema §2 strings when the registry is missing; measured today (estimate-level): `addRegistry` simulates successfully from arbitrary addresses, i.e. registry creation is open and creator-becomes-admin is the only gate. Write probes use `eth_estimateGas` so a wallet learns about a missing editor grant before signing.
- **Measured: the public testnet RPC serves permissive CORS** (reflects Origin on OPTIONS and POST). A fully static, serverless lookup page is therefore possible later; the demo still routes reads through the local server to keep one codec.
- Verified live this session: Mata ECF 24 fixture → 31 occurrences, Varghese `925 F.3d 1339` NOT_FOUND in 222 ms with the corpus-whitelist source reporting census-exact counts (218,775 / 41,988); the tx Albert flagged as unreadable on Blockscout (`0xf69dad…`) decodes to `addRecord(us-scotus, "33 S. Ct. 352", Porto Rico v. Rosaly y Castillo, 1913)`. 16 webapp tests added (extraction, whitelist/collision lookup, all five statuses live, receipt ladder + overflow refusal, calldata round-trip, server CSP/traversal/local-only-check); suite 421 green. `.claude/launch.json` added for the preview harness. Phase 3/4 boxes deliberately NOT ticked: this is a demo built on the locked schema surface, not the verifier/receipts implementation.

## 2026-06-12: Webapp UI redesigned from the Claude Design handoff (same day, Albert-driven)
The plain first-pass UI was redesigned via Claude Design (brief at docs/design-prompt.md; the designer's handoff notes vendored at docs/design-handoff.md; transcript fact worth keeping: the design session ran on questionnaire defaults and never saw the real app.js, so its demo driver reconstructed DOM shapes from the brief's class inventory). Integration choices:
- **index.html + app.css adopted from the handoff** with production fixes the designer could not know: dev-only Google-Fonts/demo-driver lines replaced with `/app.js defer`; invented placeholders corrected (precompile `0x…0808` → `0x…0A00`, fictional RPC host in the replay example removed, setup-box snippet replaced with the locked schema §2 creation strings, "runs entirely in your browser" softened to the honest "runs on this machine… in memory" claim); eyecite/reporters-db/courts-db attribution restored; ARIA wired per the handoff (tablist/tab/tabpanel roles, aria-selected toggled by JS, aria-live on busy regions and anchor-status, labeled copy buttons). Open follow-ups from the handoff: arrow-key tab navigation, `prefers-color-scheme: dark`.
- **app.js render builders rewritten to the design's DOM vocabulary** (logic, API calls, and wallet flow unchanged): title-case chip labels with CSS mask glyphs, kv rows with copy/hint affordances, doc-title cards, reg-line/reg-link/source-tag/collision-note registry cells, rb-head result banners, rc-head receipt cards, structured size meter, five-column prepare diff table, and in-place fill of the static setup-box (its button rewired via cloneNode). Contract verified mechanically post-rewrite: all 64 ids present, every JS-constructed class exists in the CSS, suite 421 green, full HTTP smoke against live testnet identical to pre-redesign.
- **Fonts self-hosted under the strict CSP** (handoff "action required"): latin woff2 subsets of Source Serif 4 (roman+italic variable), Public Sans (variable), IBM Plex Mono (400/500/600) fetched from Google Fonts, ~302 KB total, served same-origin from the static dir; `font-src 'self'` added to the CSP and `.woff2`/`.txt` to the server's type map. All three families are OFL 1.1; license texts vendored at static/fonts-OFL.txt (OFL redistribution condition). These are static assets, not Python dependencies; recorded here in the dependency spirit of the convention.
- The design bundle's demo/ state-driver was deliberately NOT shipped (it replaces app.js); it remains in the bundle if a state-gallery QA page is ever wanted.

## 2026-06-13: Tranche-1 load COMPLETE and reconciled CLEAN; indexer hardened against transport failures
The single-key load finished after ~1.5 days wall clock. Final: 260,763 records on testnet (us-scotus 218,775, us-ca11 41,988), ~96k gas/record average, ~1,201 wmantraUSD total spend (right on the 1,129-1,819 census projection), 0 failed / 0 reverts across the whole run. Sustained ~2.1 tx/s after the mid-flight optimization, submission-bound (not chain-bound) the entire time.

RECONCILE CLEAN: a full independent re-read of both registries from chain (chain_index.sqlite, offset-paged records() pinned to one block) diffed against load_state shows matched=260,763, missing=0, extra=0, drift=0. This is task 2.6's exit criterion met and the public-auditability claim made executable: the registries can be reconstructed from an RPC URL alone and they match what we intended to write, field for field.

INDEXER BUG FOUND AND FIXED (task 2.3 hardening): the first full sync crashed at ~113,600/260,763 rows on http.client.IncompleteRead -- the public RPC truncated a response mid-stream, and the indexer (unlike the loader) had no transport retry, so one blip aborted a ~1,300-page pull. Reconcile against the partial index correctly reported the gap (extra=0, drift=0 on everything synced -- proving the LOAD was never in question, only the read). Fix: transport-level retry moved INTO EvmRpc (chain/rpc.py) as OPT-IN behavior -- default max_attempts=1 (fail fast, so interactive callers like the webapp status probe and the Phase 3 verifier surface a dead RPC immediately rather than stalling through backoff), and the bulk pullers (indexer, and any third party running rebuild-index) pass max_attempts=8 with exponential backoff + an on_retry log hook. A JSON-RPC RpcError is never retried (the node answered); only OSError/HTTPException transport failures are. Pinned by tests/test_rpc.py (5 cases incl. IncompleteRead, timeout, no-retry-on-RpcError, give-up-after-max). Resync from the checkpoint then reconciled CLEAN. NOTE: the loader keeps its own equivalent rpc_retry wrapper (predates this); harmless redundancy now, a candidate for dedup onto the client's built-in when the loader is next touched.

MAINNET COST EXTRAPOLATION (task 2.6 deliverable): tranche 1 cost ~1,201 wmantraUSD on testnet for 260,763 records (~0.0046 token/record at 45 gwei, ~$1 peg). Mainnet (chain 1611, wmmUSD, same precompile + 40 gwei floor) extrapolates to roughly the same token cost for the same record set, PENDING confirmation of the mainnet gas price/peg before any mainnet load is budgeted. Full SCOTUS including the deferred tranche 2 (memoranda + the excluded vendor/specialty reporters, ~190k more distinct keys per the census) would roughly double the SCOTUS portion. Throughput: at the measured client-bound ~2.1 tx/s a mainnet tranche-1 load is ~1.5 days single-key; a hardened mainnet loader could go faster with connection pooling / parallel keys (the 1.1 tx/s "chain plateau" was disproven -- it was client overhead).

WEBAPP follow-up (out of session scope, flagged): the webapp StatusService makes an eager RPC probe at server construction; when the RPC is slow/unroutable it blocks for the full 30s EvmRpc timeout (observed as a 35s setup stall in test_webapp.py against the deliberately-unroutable 127.0.0.1:9). Verified PRE-EXISTING (reproduces on committed code with this session's changes stashed), not caused by the retry work. Fix deferred to its own task: short timeout or lazy/cached probe for the interactive status path.

## 2026-06-13: Daily updater (task 2.7) - cursor is date_MODIFIED, not date_created (probe-driven)
loader/update.py keeps the registries current between quarterly bulk loads via the CourtListener REST API v4 clusters LIST endpoint. Four live probes settled the design before any code (same verify-reality discipline as the CSV-dialect catch):
- A freshly published opinion has an EMPTY citations list; the reporter cite (volume/page) is added LATER as a modification to the cluster. So the plan's "date_created cursor" would systematically MISS every citation we key on. Cursor is therefore cluster `date_modified` (normalized to UTC), stored per registry in load_state meta (`update_cursor:us-*`).
- CL exposes NO citation-level filter (`citations__date_created__gte` -> HTTP 400) and NO standalone citations endpoint (`/citations/` -> 404), so citation-granular cursoring is impossible; we page clusters by date_modified ascending and let idempotent INSERT-OR-IGNORE absorb the churn (most modified clusters have no new in-scope cite: a live sample showed 184 ca11 / 29 scotus clusters modified in ~6 weeks, 0 with a new whitelisted key).
- Embedded citation shape confirmed: {volume, reporter, page, type:int, date_created, date_modified}. Reporter whitelist is the SAME as tranche 1, now a single source of truth in loader/scope.py (REGISTRY_REPORTERS).
Design: append-only and idempotent (each in-scope cite -> one single-case record; parallel cites -> multiple records sharing the cluster; existing keys untouched). Ascending date_modified + inclusive gte + `--max-clusters` cap => surge-safe and resumable (a CL-side mass re-index spreads across runs). `--dry-run` writes nothing (no rows, no cursor). `--since` override; default first-run cursor = corpus snapshot date. `--submit` runs the chain load after appending (gated by the present-plan-before-tx rule; not used unattended in a session). The CL client is stdlib urllib with token auth, polite inter-page sleep, and retry/backoff honoring HTTP 429 Retry-After. EXPLICITLY NOT USED: the citation-lookup API (spot-check oracle only, per the plan).
OUT OF SCOPE for the daily delta (remain the quarterly bulk reload + reconcile's job): incremental collision MERGES (a new case on an existing key's first page) and metadata corrections to existing keys; both need a superseding version the append deliberately does not write.
Live validation 2026-06-13: `update --dry-run --since 2026-05-01 --max-clusters 400` ran end to end against the real API - paging, filtering, rendering, dry-run accounting, cursor computation, AND a real HTTP 429 (slept 47s per Retry-After) and a transient timeout retry all exercised; 0 new keys (correct - cites for post-snapshot opinions haven't been assigned yet). 10 unit tests with canned JSON (no network). No new dependency (stdlib only). New: loader/scope.py, loader/update.py, tests/test_update.py.

## 2026-06-13: Phase 2 COMPLETE (tag phase-2-done)
All of 2.1-2.7 done. Exit criteria met: us-scotus (737) + us-ca11 (738) populated on testnet with 260,763 tranche-1 records; reconcile CLEAN (0/0/0 vs a full independent chain re-read); throughput + cost table in DECISIONS (the load entries above). Tranche 2 (SCOTUS memoranda + excluded vendor/specialty reporters) stays deferred by choice, not budget. Next session: Phase 3 (Verifier) - `nvnm-cite check`, local-index-only by default, no chain trace at drafting time.

## 2026-06-13: Frontend workstream merged; product architecture amended (web-demo review)
Source: docs/webapp-revision-notes.md (handoff from the "nvnm-cite frontend explorer" session) + Albert's decisions in the main thread. The redesigned web demo (src/nvnm_cite/webapp/) is already on main (commits 2200e32, 41e00c3) and pre-implements much of Phases 3-4 in web form, so "merging the workstreams" is a plan/architecture reconciliation, not a git merge. All items settled in-thread:

ITEM 0 (LEAD) -- Drafting-time checks READ THE CHAIN LIVE; reverses CLAUDE.md invariant 3. The check resolves each citation via a live keyed records() eth_call against the NVNM-operated RPC, not a hosted local copy -- otherwise users trust us, not the chain. Privacy is preserved because WE operate the RPC: eth_call reads are point-to-point (no mempool, no block, no on-chain trace), so the citation set is visible only to NVNM-as-operator, identical to the in-memory parse we already do, and never published. The surviving win is NON-REPUDIATION: we cannot lie about the chain's answer, because any skeptic replays the exact records() call against an independent node. The local index (chain_index.sqlite) is DEMOTED to a rebuildable audit/cache (rebuild-index) + optional perf cache -- never the lookup authority. The chain-read path already exists (webapp ChainGateway.keyed_record, keyed-miss handling); adopting it is mostly removing CheckService's local lookup. Amends invariant 3 and Phase 3 task 3.2.

ITEM 1 -- Hosted privacy model. "Nothing leaves this machine" is false hosted (true only on localhost). Honest model: document uploaded, parsed in memory server-side (eyecite stays the single reference normalizer, invariant 5), citations resolved by live chain read, document discarded with the response -- never written to disk, never on chain. Check-tab copy rewritten.

ITEM 2 -- Drop kya_id. Identity IS the signing wallet (one wallet per agent). Receipt agent = {address} only; remove the free-text input and the kya_id field; show "Attesting as 0x..." read-only.

ITEM 2b -- Receipts do NOT enumerate cited cases (Albert's call). The receipt's document_sha256 binds the exact file immutably, so anyone with the file reproduces the verdicts (re-run extract->normalize->live-read pinned to checked_at_block); listing cases is redundant AND would publish the brief's authorities (its litigation strategy) on a public permanent chain pre-filing. So NO enumeration, not even opt-in -- the confidentiality sub-question is mooted by removal. Receipts keep a NON-IDENTIFYING STATUS TALLY ({checked, verified, not_found, not_covered, ...}) for human readability. CONSEQUENCE: a receipt is ~480 B, always under the 2048 B cap, so the chunked-receipt design (Phase 4 task 4.3) and the webapp compaction ladder are DROPPED. Case-frequency analytics come from aggregate RPC query telemetry (item 0 makes every records() lookup observable to us as operator), aggregated BY CITATION and decoupled from document hash / client identity to keep the privacy claim honest; this retention is disclosed in the privacy copy.

ITEM 3 -- Receipts live in PER-FIRM-PER-CASE registries; NO global registry. Open/public-write registries are impossible (addRegistry has no access param; grantRole targets a specific address -- confirmed from the vendored ABI), so a shared global registry would force NVNM to grantRole every attester forever = permanent gatekeeper, contradicting invariant 4. Model: one registry per (firm, case) -- "Firm XYZ's work on ABC case = registry N" -- owned by the filing party's own wallet (creator=admin, self-sovereign writes), matching how legal work is partitioned (many parties per case, many cases per lawyer). DISCOVERY: the filing carries "Citation verifications: <link to registry N>". This is the registry LINK -- a STABLE pointer established when the matter opens, BEFORE any document is hashed -- NOT the receipt/tx (which is circular: the receipt is keyed by the final document hash, so referencing it would change the bytes and thus the hash; this circular approach was proposed and rejected). A verifier follows the link, hashes the filing locally, and does a keyed records(registry, hash) lookup. Pilot: the demo agent creates its own per-firm-per-case registry (replaces the never-created global receipts-v1). FORWARD REQUIREMENT: the link lands a verifier on a registry VIEW that must be legible to a non-technical clerk; we control our webapp's registry view (not Blockscout's UI), so a clear registry view is a web-app-phase requirement; the Verify flow now takes (registry + file), not hash-alone. Supersedes the receipts-v1 single-registry model in docs/record-schema.md sec 2/4 (re-locks at Phase 4 task 4.1; clean -- no receipts written yet).

ITEMS 4-6 -- Copy/UX. Verify tab copy stays (browser WebCrypto hashing; only the 64-char hash leaves the browser -- survives the hosted move). Inspect: drop jargon ("mojibake", "ABI framing"), describe plainly. About: newcomer-friendly explainer + lawyer FAQ, numbers live from /api/status. Draft copy in webapp-revision-notes.md.

PLAN IMPACT: Phase 3 re-scoped (live reads; shared verifier core called by CLI + webapp so eyecite stays the one normalizer; extraction/normalize/status logic already in webapp/). Phase 4 slimmed (lock minimal non-enumerating receipt v1; drop kya_id; drop chunked receipts 4.3; per-firm-per-case registry create + cite-the-registry discovery; verify takes registry+file; add RPC analytics telemetry). New Phase 4.5 (web app: item 0 wiring, hosted in-memory model, copy rewrites 1/4/5/6, clear registry view, StatusService fast-fail). P5 (MCP) + P6 (demo) inherit the live-read default; the Mata demo becomes browser-runnable.

## 2026-06-14: Phase 3 scoping answers (Albert, via the three plain-English questions)
Three calls settled before any Phase 3 code, in answer to the kickoff questions:
- **A — update both surfaces now.** The shared verifier core is adopted by BOTH the new CLI and the existing webapp in this session (folds task 4.5a in), not deferred. So there is one verification path immediately, no divergence window.
- **B — pdfplumber becomes a RUNTIME dependency** (was dev-only). A brief is almost always a PDF, so `nvnm-cite check brief.pdf` must work out of the box. Moved from `[dependency-groups].dev` to `[project].dependencies` in pyproject (version/license already recorded 2026-06-11; this is a surface change, not a new dep). The import in extract.py stays guarded so the package still loads without it (honest "upload .docx/.txt" error).
- **C — coverage is a FIXED built-in list** (`us-scotus`, `us-ca11`), NOT live registry-existence. A citation mapping outside the list is NOT_COVERED with no chain read; expanding coverage is a one-line change in `verifier/check.py` (`COVERED_REGISTRIES`) plus a test. Chosen for predictability over auto-discovery of third-party registries (the invariant-4 auto-reach is a deliberate non-goal for the pilot).

## 2026-06-15: Phase 3 COMPLETE — shared live-read verifier core + `nvnm-cite check` CLI (tag phase-3-done)
The verifier is a new package `src/nvnm_cite/verifier/` (extract.py, resolver.py, check.py) called by BOTH the CLI and the webapp, so eyecite stays the one normalizer (invariant 5) and the five-status logic exists once. Tasks 3.1–3.3 done; 4.5a folded in. 461 tests green (was 436); validated live against testnet via the CLI, the webapp server, and a gated integration test.

- **Live reads (item 0), implemented.** `verifier/resolver.py::ChainResolver` does a live keyed `records(registry, citation)` eth_call per distinct covered citation and returns a `Resolution(record, query)`. The core (`check.py`) maps record→VERIFIED, keyed-miss→NOT_FOUND, registry-not-in-`COVERED_REGISTRIES`→NOT_COVERED (no read), plus AMBIGUOUS/UNPARSEABLE from the normalizer. `name_check` reads the LIVE record's metadata. The local index is no longer consulted by a check.
- **THE invariant: transport ≠ NOT_FOUND.** NOT_FOUND comes ONLY from the precompile keyed-miss (`collections: not found`); every other failure (transport, timeout, non-miss RpcError) PROPAGATES. The resolver enforces it; the CLI exits 2 ("could not reach NVNM Chain…"), the webapp returns 502 (server now catches `OSError`→502, ordered after the BrokenPipe/ConnectionReset `pass`). Pinned by `test_verifier.py` (RaisingResolver) and `test_webapp.py::test_server_check_surfaces_dead_rpc` (unroutable RPC → 502, never a fake NOT_FOUND).
- **Non-repudiation surfaced.** Each keyed result carries `query` = the exact replayable `eth_call` ({to: precompile, data}); NOT_COVERED/AMBIGUOUS/UNPARSEABLE carry `query: null`. Present in CLI `--json` and the webapp result; visible rendering of the chip in the Check UI is left to the 4.5 pass.
- **`is_keyed_miss` centralized** in `chain/precompile.py` (`KEYED_MISS_MARKER` + duck-typed `is_keyed_miss(err)`), the single source the resolver and the webapp `ChainGateway` both use; the webapp's private `_is_keyed_miss` was removed.
- **CLI.** `src/nvnm_cite/cli.py` + `__main__.py`; console script `nvnm-cite` in pyproject. `nvnm-cite check <file> [--rpc] [--block] [--json]` renders a status table + summary + an existence-only disclaimer; read-only (eth_call only, no writes, no gas). Subcommand scaffold for Phase 4 (anchor/verify/…). `uv run nvnm-cite check` validated live: Roe 410 U.S. 113 → VERIFIED/match, Brown 347 U.S. 483 paired with a fake name → VERIFIED/MISMATCH, Varghese 925 F.3d 1339 → NOT FOUND, ca2 cite → NOT COVERED, court-less F.3d → AMBIGUOUS.
- **Webapp rewired (4.5a).** `CheckService(resolver)` (was `CheckService(LocalIndex)`) delegates to `check_document`; server constructs `CheckService(ChainResolver(lambda: EvmRpc(rpc_url)))` (fresh client per call = thread-safe). `LocalIndex` demoted to the status-panel coverage source + `rebuild-index` audit cache (docstrings updated; CLAUDE.md invariant 3 rationale superseded in webapp/__init__.py). Honest privacy copy: the server-generated check note no longer claims "no chain request," and the static Check-tab callout ("Nothing leaves this machine") was corrected to the live-read truth. The Verify-tab browser-hashing claim stays true and untouched (item 4). Broader Check/About/Inspect/FAQ copy + the registry view remain Phase 4.5.
- **eyecite chatter quieted.** `logging.getLogger("eyecite").setLevel(logging.ERROR)` in `normalizer/canonical.py`: eyecite's resolution `logger.warning` ("Unknown overlap case" for an Id. next to a full cite) is not a signal we act on (output is golden-pinned) and was leaking to the terminal/server log. ERROR keeps real errors visible.
- **Extraction recall (task 3.1).** `tests/fixtures/briefs/recall.json` records the FULL pipeline (PDF extract + normalize) over the RECAP fixtures; `test_verifier_recall.py` recomputes and checks (occurrence/distinct as floors, key-cite counts exact, scanned ECF 21 → 0 with the scan warning). Measured: ECF24 31 occ / Varghese×1; ECF54 97 occ / Varghese×2; Walters {us-ca11,us-scotus}; ECF21 scan 0.
- **Tests added:** test_verifier.py (core, fakes), test_cli.py (CLI render + exit codes), test_verifier_recall.py (PDF recall, pdfplumber-gated), test_verifier_live.py (testnet, RPC-gated). test_webapp.py check tests switched to a fake Resolver; extract import repointed to verifier.
- **Read-only session:** no chain writes, no transactions, no gas. Anchoring (writes) begins in Phase 4.

## 2026-06-15: Receipt v1 + receipt-registry naming RE-LOCKED (Phase 4 task 4.1)
`src/nvnm_cite/receipts/schema.py` locks the minimal, non-enumerating receipt and the per-firm-per-case receipt registry, superseding the global `receipts-v1` model (docs/record-schema.md §2/§4 re-locked to match). Pure module + tests (tests/test_receipts.py); no chain writes; 468 tests green. Built as a checkpoint head-start on Phase 4 after Phase 3 wrapped; the rest of Phase 4 (writes) is deferred to a focused session per the one-phase-per-session cadence.
- **Receipt v1 (`nvnm-cite-receipt/v1`)**: `{agent:{address}, chain_id, checked_at_block, document_sha256, normalizer_version, registries:[{id,name,head_block}], schema, summary, timestamp}`. `summary` is a non-identifying tally: `checked` (distinct citations) + verified/not_found/not_covered/ambiguous/unparseable + name_mismatches. NO per-case list (item 2b); NO kya_id (item 2 — identity IS the wallet). ~480–700 B compact, always under the 2048 B cap, so the chunked design (task 4.3) stays dropped. Serialization per schema §3.2 (sorted keys, no whitespace, UTF-8).
- **Registry naming (Albert 2026-06-15, "filer picks a readable name")**: one registry per (firm, case), owned by the filer's wallet (creator=admin; invariant 4, no global gatekeeper). Name = `<firm-slug>--<case-slug>`, lowercase `[a-z0-9-]`, `--` separating firm from case, ≤64 B (`receipt_registry_name`). Readable for a clerk/judge following the discovery link; `--` never collides with a court id (`us-*`). Locked creation strings (description + metadata with firm/case/kind/schema/spec) in `receipt_registry_strings`. The auto-generated-machine-name alternative was rejected for legibility.
- **Discovery** stays the registry LINK printed on the filing (item 3), not the receipt/tx (circular). `verify` takes (registry + file) — built in task 4.4.
- **Deferred to the next session (chain WRITES, explicit per-tx OK):** 4.2 anchor (create the demo `inveniam--mata-v-avianca` registry + write a receipt), 4.4 verify, 4.5 CLI anchor/verify wiring, 4.6 telemetry, and the webapp receipt-flow update to v1.

## 2026-06-15: Phase 4 anchor + verify built and round-tripped LIVE on testnet (4.2/4.4, 4.5 partial)
Implemented `src/nvnm_cite/receipts/{anchor,verify,chainio}.py` and wired `nvnm-cite anchor|verify`. Hermetic tests (injected reader/resolver + fake RPC) in tests/test_receipts_flow.py + tests/test_cli.py; 479 green. Then exercised LIVE end to end (Albert approved the two writes in-chat, per the present-plan-before-tx rule). This continued in the same session as Phase 3 + 4.1 at Albert's direction ("proceed"), setting aside the usual one-phase-per-session cadence.
- **anchor.py**: `prepare_anchor` is READ-ONLY — pins the check to one block (receipt reproducibility), assembles the registries-read table + minimal receipt + exact addRegistry/addRecord calldata, and reports registry existence + already-anchored. `send` performs the WRITES (addRegistry if missing, then addRecord), strictly sequential, signing with the from-scratch signer. The CLI `--anchor` flag gates the send; without it the command is a dry-run plan. `receipts/chainio.py::ChainReader` gives receipts/ its chain reads without importing webapp/ (clean layering; a dedup candidate with the webapp ChainGateway).
- **verify.py**: (registry + file) → hash locally → keyed `records(registry, sha)` read. A miss = NO RECEIPT (the tamper signal: one changed byte changes the fingerprint). On a hit it re-runs the check pinned to the receipt's `checked_at_block` (archive read) and confirms the recomputed tally matches the stored tally. Verdicts: verified / summary_drift / found / not_found / registry_not_found / bad_receipt; exits nonzero unless cleanly verified.
- **LIVE artifacts (testnet 787111) — the session's non-repudiation record:** registry `inveniam--mata-v-avianca` created at block 1,692,206 (tx 0xf86554ba54997ff65e430a05e315ff95cb37acf4de35da4f95e6d0e5848be932, 77,235 gas; creator/admin = load wallet 0xaf63…7EcB). Receipt for the Mata v. Avianca ECF24 reply memo (doc SHA-256 7d563cfc…1409f9) anchored at block 1,692,207 (tx 0xdb9b1508063f9851660077f5d30822758332f22a9fbdf7ec66587e4d32509a11, 123,530 gas); receipt 526 B; tally checked=27 / verified=6 / not_found=2 / not_covered=14 / ambiguous=4 / unparseable=1. The fabricated Varghese 925 F.3d 1339 (and a second absent ca11 cite, 516 F.3d 1237) returned NOT_FOUND. `verify` on the file → VERIFIED (recomputed tally matches); a one-byte-tampered copy → NO RECEIPT.
- **Phase 4 anchor/verify exit criteria met** (real RECAP brief round-trip; tamper caught; cold third party verifies from registry + file). REMAINING in Phase 4: 4.6 RPC query telemetry, the rest of the unified CLI (sync/rebuild-index/reconcile/stats/load), and the webapp receipt-flow update to v1; the clerk-legible registry VIEW is the webapp's job (Phase 4.5c).

## 2026-06-15: Phase 4 core complete — unified CLI (4.5) + query telemetry (4.6)
- **Unified CLI (4.5)**: `nvnm-cite` exposes `check`/`anchor`/`verify`/`stats` as structured subcommands and DELEGATES the operator verbs `sync`/`rebuild-index`/`reconcile`/`load`/`update` to their existing module mains (intercepted in `cli.main` before argparse, so `<cmd> --help` shows the module's own flags and `nvnm-cite --help` lists them in an epilog — no logic duplication). `stats` reuses `indexer.index_stats`, reporting records/registry at the stated sync head, never the precompile's unreliable countTotal; live it shows us-scotus 218,775 + us-ca11 41,988.
- **Query telemetry (4.6)**: `verifier/telemetry.py` — `SqliteTelemetry` (thread-safe via a lock + check_same_thread=False) + `NullTelemetry`. The store holds ONLY `(registry, citation) → (lookups, hits)` with timestamps; a schema test pins that no document/identity column exists, keeping the privacy claim un-joinable. Hooked into `ChainResolver`: records on a hit or a keyed-miss, NEVER on a transport error. Off by default; opt-in via `nvnm-cite check --telemetry <path>` (live-validated: 410 U.S. 113 hit, 347 U.S. 483 hit, 925 F.3d 1339 miss). Webapp enablement + the disclosure copy (item 2b) fold into Phase 4.5; the sink is already thread-safe for the threaded server.
- **Phase 4 is functionally complete (487 green) but NOT tagged phase-4-done**: per the 2026-06-13 re-scope the webapp receipt flow + the clerk-legible registry VIEW live in Phase 4.5, so the tag waits for that. This long session covered Phase 3 + the Phase 4 core at Albert's direction, setting aside the one-phase-per-session cadence.

## 2026-06-15: Phase 4.5 web app COMPLETE — webapp receipt flow rewired to the locked v1 (tag phase-4-done)
The webapp (`src/nvnm_cite/webapp/`) receipt path was migrated off the old draft (global `receipts-v1`, `nvnm-cite-receipt/v1-draft`, `kya_id`, the compaction ladder, enumerated `results`) onto the LOCKED receipt v1, so the demo surface now matches the CLI and the locked schema. Tasks 4.5a–4.5e done; 488 tests green; verified LIVE end to end in a real browser against testnet (all 5 tabs, zero console errors). No transactions sent (read-only verification; the demo receipt already existed from the Phase 4 session). Settled choices:
- **The webapp MIRRORS the CLI by delegation, not re-implementation.** `ReceiptService.prepare` now delegates to `receipts/anchor.py::prepare_anchor` and `lookup` does a keyed `records(registry, hash)` read; `build_receipt`/`ReceiptTooLarge`/the enumerating prepare were deleted from `webapp/service.py`. The webapp receipt is byte-identical to `nvnm-cite anchor`'s (verified live: 526 B, same tally).
- **Prepare RE-UPLOADS the document** (POST `/api/receipt/prepare` is now file bytes + `X-Filename`/`X-Firm`/`X-Case`/`X-Agent` headers, not JSON). This is FORCED, not a preference: `receipts/verify.py` recomputes the tally by re-running `check_document` at `checked_at_block`, so prepare must produce the stored tally the same way (a real pinned re-check of the exact bytes) or verify would drift. The browser retains the checked file in page memory (`lastFile`) and re-sends it; parsed in memory, discarded with the response — same posture as `/api/check`. Trusting browser-sent results/hash was considered and rejected (less honest, can drift from verify).
- **The Verify tab stays hash-only + registry (item 4 preserved).** Inputs are (registry name from the filing's discovery link + the file); the file is SHA-256'd in the browser (WebCrypto) and only the 64-char hash + registry name leave the browser. The pinned RECOMPUTE that `verify_document` does (needs the bytes) is the CLI `nvnm-cite verify`'s job; the webapp Verify is the free, no-upload existence+tamper check and the clerk-legible registry VIEW (item 3 forward requirement) — a non-enumerating receipt card (attester / checked_at_block / registries read / tally). `/api/receipt/lookup` takes `registry` + `sha256`.
- **Telemetry: off by default, opt-in via `python -m nvnm_cite.webapp --telemetry <path>`.** When on, one shared `SqliteTelemetry` feeds the check `ChainResolver`; `/api/status` exposes `telemetry.enabled`, and the Check privacy callout renders the disclosure (item 2b). Verified live: the sink recorded by-citation `(registry, citation, lookups, hits)` only — no document/identity column.
- **StatusService fast-fail (4.5e)** = a dedicated SHORT-timeout gateway (`STATUS_RPC_TIMEOUT = 4 s`) for the lazy+cached status probe, distinct from the 30 s default the check/receipt paths keep. The earlier "eager probe at server construction" was already removed in Phase 3; the remaining stall was the long timeout on the lazy probe. Pinned by `test_server_status_fast_fails_on_dead_rpc`.
- **Copy (items 1/4/5/6):** Inspect drops "mojibake"/"ABI framing"/explorer-disparagement ("transaction reference", "stored as plain text"); About is the newcomer explainer + 8-question lawyer FAQ with the coverage figure rendered live from `/api/status` (never hard-coded); the Check tab renders the per-citation replayable `eth_call` (4.5a tail). The status panel no longer probes or shows a global receipts registry (none exists in the per-firm-per-case model).
- **Latent JS bug found by the live browser pass and fixed:** `banner(tone, icon, title, "")` did not create the `.rb-sub` element, but `renderLookup`'s found-branch and `anchorReceipt`'s revert-branch both call `b.querySelector(".rb-sub").appendChild(...)` → "Cannot read properties of null". Latent because the old global `receipts-v1` was never created, so the found/revert paths never ran. Fix: `banner()` always appends `.rb-sub` (empty when no subText). This is exactly why the browser pass matters — hermetic tests + the response-shape checks all passed; only running the real DOM caught it.
- **No new dependencies.** `--telemetry` reuses the existing `SqliteTelemetry`; everything else is stdlib + the existing codec. The `.claude/launch.json` got an explicit `cwd` (project root) — harmless, though the Claude Preview harness still faulted on its own cwd, so the browser verification ran via a backgrounded `python -m nvnm_cite.webapp` driven through the Chrome extension.
- **Phase 4 + Phase 4.5 COMPLETE → tag phase-4-done.** Next session: Phase 5 (MCP server) per the one-phase-per-session cadence.

## 2026-06-16: CourtListener citation-completeness gap + test-pilot backfill
Surfaced when a test brief's *Muransky v. Godiva Chocolatier, Inc.*, 979 F.3d 917 (11th Cir. 2020) (en banc) — a real, leading en-banc standing case — came back NOT_FOUND in the webapp.
- **Root cause (not our bug):** the normalizer parses `979 F.3d 917` → `us-ca11` correctly and the live chain read correctly misses. We HAVE the opinion cluster (4801215, Published, 2020-10-28) but CourtListener never attached its West reporter citation — confirmed against CL LIVE (cluster `citations: []`) and the citation-lookup oracle (**404**). Our registry is keyed by reporter citation, so a real case whose CL record lacks that cite is absent → an honest-but-misleading NOT_FOUND.
- **Scale:** ~2,195 (4.1%) of Published ca11 clusters and ~155 (0.0%) of SCOTUS have no reporter cite in CL. But MOST of the ca11 gap is not a real false-NOT_FOUND: a large share are recent decisions awaiting a West cite (samples dated 2026-03, days before the snapshot — the cite does not exist anywhere yet, and you cannot cite a nonexistent cite) or genuinely uncited (table/slip). The fixable class is OLDER cases whose West cite exists but CL lacks it (Muransky). Identifying/sourcing those at scale needs a second authoritative dataset (e.g. Caselaw Access Project) — deferred to mainnet Phase 7.1.
- **Decisions (Albert, 2026-06-16):** (1) NO disclosure copy — this is a test-only pilot nobody relies on. (2) Handle the symptom by TARGETED backfill: add specific real cites as they surface in testing, each verified; do NOT bulk-add (most of the gap has no cite to add) and NEVER invent a cite (fabricating cite→case mappings is the exact failure this product catches). (3) The mainnet answer is its own Phase — added **Phase 7: Mainnet production plan** (task 7.1 = data-coverage completeness, the load-bearing trust issue).
- **Mechanism:** `scripts/backfill_supplemental.py` + curated `scripts/supplemental_citations.csv`. It reuses the LOCKED record schema (`loader/records.render_record`) and the from-scratch signer, writing into the existing `us-*` registry signed by the load key (its admin). Only the missing cite→cluster LINK is supplied; name/year/uri come from the CL cluster we already hold, so provenance stays "existence per CL's own cluster data." Idempotent (skips cites already on chain), keeps `chain_index` + `corpus` consistent so `stats`/`reconcile` stay accurate, `--dry-run` previews. The CSV is the auditable, re-appliable list of everything added beyond CL.
- **Applied LIVE:** Muransky `979 F.3d 917` → `us-ca11` (tx 0xf8e6c657…6054a4, block 1,716,818, 88,068 gas ≈ 0.004 wmantraUSD). Verified VERIFIED on the live chain; synthetic cites in the same brief (e.g. 81 F.4th 1174) correctly stay NOT_FOUND. NOT a mainnet path.

## 2026-06-24: Full-scope decision (all US case law) + measured scale/cost; mainnet gas confirmed live
Exploration session (NO chain writes). Re-scoped the mainnet target from the federal-appellate backbone to the FULL US case-law citation graph; measured its scale/cost from CourtListener bulk data; confirmed the cost model against live mainnet (read-only). Written up in IMPLEMENTATION_PLAN Phase 7 (rewritten) + the Future-development wishlist, docs/proposal-mainnet.md (internal funding ask), docs/mantra-brief.md (MANTRA chain-dev brief).
- **Scope DECIDED (settles the old Phase 7.4 "which courts" question):** all federal (incl. ~94 districts, bankruptcy, specialized) AND all state courts, loaded in tranches. WHY full, not federal-appellate-only: (1) coverage breadth = anti-hallucination power — an uncovered court returns NOT_COVERED for a fabricated cite, indistinguishable from real-but-uncovered, so a narrow scope lets fabrications hide; (2) ~90% of US litigation is in STATE courts, so a federal-only service is not "relevant for real-life legal use." Same pipeline + data source (CourtListener/CAP holds essentially all published US case law) + registry model; the genuinely new engineering is the state-reporter normalizer (Phase 7.7).
- **Full-scope census** (scan of the CourtListener citations bulk, snapshot 2026-03-31, bucketed by CL citation type): **~7,837,721 distinct cases** (clusters with >=1 citation); **~11,912,016 registry-eligible citation records** = distinct citations under real reporters (CL types 1/2/3/5/8), EXCLUDING WL/LEXIS/specialty vendor cites (4/6/7) per the pilot rule. Split: **federal 1,469,892 (~12%) / state 10,442,192 (~87%)**. Counts are raw-distinct on (volume|reporter|page); the loader's canonical dedup trims modestly. State is dominated by official reporters (type 2 = 7.43M distinct) + regional (So.2d 374k, P.2d 267k, S.W.2d 256k, N.E.2d 246k, A.2d 245k, S.E.2d 206k, N.W.2d 156k).
- **Federal appellate backbone (tranche 1), EXACT:** **924,421 distinct canonical records** across SCOTUS + all 13 Courts of Appeals (F.2d/F.3d/F.4th/F. App'x + the scotus reporters). Method validated by reproducing the locked testnet load set to the unit (scotus 218,775, ca11 41,988). Adding the older circuits' first-series `F.` -> 954,043 (immaterial). Per-court table in docs/proposal-mainnet.md Appendix B; built via `loader.courtlistener --courts scotus,ca1..ca11,cadc,cafc`.
- **Mainnet gas CONFIRMED live (chain 1611, read-only — no key, no write):** `eth_gasPrice` = **45 gwei**, identical to testnet; `eth_estimateGas` for a novel `addRegistry` = **83,541 gas, identical to the unit on both networks** -> the deployed mainnet precompile is the SAME binary as testnet. Both gas drivers (price + per-record gas) carry over CONFIRMED, not extrapolated. At the measured $0.0046/record: tranche-1 ~= $4,252; all-federal ~= $6,800; all-state ~= $48,000; **FULL scope ~= $54,800**. NVNM gas is fixed-floor (40 gwei floor, 45 suggested), not auction-priced, so cost is deterministic. THE ONE OPEN VARIABLE: the wmmUSD/USD peg (assumed $1; not chain-readable) — cost scales linearly; confirm internally before any mainnet budget sign-off.
- **Tranche ladder + the gating lift:** fed appellate -> fed complete (same reporters, no normalizer change) -> state-normalizer pilot (CA/NY/TX/FL/IL) -> all states; each gated on a PUBLISHED per-court completeness bar. The state-reporter normalizer rebuild (Phase 7.7; ~50 states' official reporters + 7 regional families + neutral/parallel cites, with an exhaustive golden suite per invariant 5) is the critical path for state coverage, NOT the chain or gas. Load is submission-bound (~2.1 tx/s/key): full scope ~65 days single-key, ~1 week with ~10-15 parallel editor-granted keys (checkpointed loader already built). One keyed record per citation is REQUIRED by the keyed `records()` existence read, so per-record storage gas dominates regardless of batching.
- **Chain feature proposals to MANTRA (wishlist, not blockers):** W1 `updateRecordStatus` + a `supersededBy` pointer — the record tuple already carries `status`/`recordId`/`index`/`isLatest` and `records()` already returns `status`, and `updateRecordStatus` is spec'd-but-unshipped; append-only, admin-gated, NEVER mutates the original content (original tx payload immutable by the chain regardless) — a general version-control / revocation primitive useful beyond NVNM Cite. W4 a native batch write (`addRecords`) for load efficiency. Detail in docs/mantra-brief.md §4 and IMPLEMENTATION_PLAN Future-development wishlist W1-W6.

## 2026-06-27: Repo cleanup
- **Purged the 8.1 GB gitignored `data/`** (bulk `.bz2` downloads, regenerable corpus/chain_index/load_state sqlite, logs, pid) + all `__pycache__`/`.pytest_cache`/`.DS_Store`: repo on disk ~8.2 GB -> 90 MB. All re-downloadable (CourtListener S3) or rebuildable (`loader` / `rebuild-index`); the testnet load is done + reconciled, so nothing was lost.
- **Removed two spent working docs:** `docs/design-prompt.md` (one-time UI design prompt; the redesign is built) and `docs/webapp-revision-notes.md` (Phase 4.5 prepared-revisions handoff; its decisions are the 2026-06-13 entries above). Earlier DECISIONS references to them are historical — the files live in git history; not scrubbed (this file is append-only memory).
- **Refreshed `docs/web-demo.md`** — it still described the pre-item-0 local-index check and the draft enumerating receipt; rewritten to live keyed reads (item 0) + the locked non-enumerating receipt v1. 488 tests green.

## 2026-07-07: updateRecordStatus + events EXIST on both networks — measured corrections to the chain model (supersedes experiment (f) and the "no events" claim)

Context: review of MANTRA's "Supersession via addRecord" counter-proposal (Notion) → email exchange with Lance (MANTRA) → live re-verification. Every probe read-only (eth_call / eth_getLogs / explorer API; no key, no write; first probes ~2026-07-02, confirmations 2026-07-07). MANTRA's interface file `anchoring-abi.sol` (received 2026-07-07, repo root) cross-checked: all seven @dev selectors match their signatures' keccak, and deployed behavior agrees everywhere it is probeable.

- **updateRecordStatus IS DEPLOYED on both networks**: `updateRecordStatus(uint64 registryId, uint64 recordId, uint64 index, string status)`, selector `0x97b40c25`. Probe from an ungranted address → `unauthorized` on testnet AND mainnet (dispatcher knows it; role gate answers). SUPERSEDES experiment (f) and the CLAUDE.md "missing from the deployed binary" line. The May 2026 nvnm-tutorial report and our own 2026-07-02 nine-signature probe made the SAME mistake: guessing name/checksum-keyed shapes because reads key that way. WRITES that target an existing record are ID-KEYED. Method lesson, recorded: selector probing only rules out the signatures actually tried; treat "absence of a guessed selector" as evidence, never proof.
- **updateRecordStatus semantics (per Lance, 2026-07-07)**: `status` is a FREE-FORM string — no chain-level enum or validation; Active/Superseded/Revoked/Invalid is purely an application convention. It is an IN-PLACE write on that (registry, checksum, index) — no new version — reversible by another write, and status is the ONLY mutable field. Auth = registry admin/editor via grantRole. Prior status values reconstructable by archive read; MANTRA states a commitment to keeping the public archival node. LIVE-EXERCISED same day (Albert's OK in-session; testnet only, dev-probe record 1): `updateRecordStatus(733, 1, 1, "Superseded")` tx `0x748cc48e…d41e` (block 2,250,125, gasUsed 43,527 — estimate exact to the unit) flipped ONLY the explicit index-1 read to Superseded while the latest version (index 4, Active, isLatest) was untouched; the receipt carries exactly one UpdateRecordStatus event (topic0 matches, caller = our wallet, data = registryId 733 + recordId/index 1). Reversal `…"Active"` tx `0x16ae6d77…c1da` (block 2,250,126, gasUsed 43,371) restored the pre-test state — REVERSIBILITY CONFIRMED. Status-write gas ≈ 43.4–43.5k (about half an addRecord).
- **revokeRole(uint64, string, address, string) is deployed on both networks** (selector `0xacd58bc7`; decode-error probe response on both). Absent from our vendored ABI until today. Operational win for Phase 7.3: editor keys can be RETIRED after each tranche instead of holding grants forever.
- **The precompile EMITS EVENTS and always did** — SUPERSEDES "emits NO events (privacy by design)" (CLAUDE.md; mantra-brief section 1; that claim was inherited, never measured — nobody ever inspected a receipt's logs). Events: AddRegistry, AddRecord, UpdateRecordStatus, GrantRole, RevokeRole; only `caller` indexed (topic1); AddRecord log data carries registryId/recordId/index/checksum (the plaintext citation — or receipt doc-hash — lands in log data). Measured: recent addRegistry (testnet) and addRecord (mainnet) receipts each carry one log; topic0 hashes match the .sol event signatures exactly; eth_getLogs over our own Phase 0 window (blocks 1,573,400–1,573,510, 2026-06-10) shows the dev-probe registry creation AND the round-trip record both emitted events; Lance's URS test txs (testnet blocks 2,213,825 / 2,213,956, registries 1665/1666) emitted UpdateRecordStatus events. PRIVACY IMPACT: invariant 3 unaffected — drafting-time checks are eth_call READS, which produce no logs; writes were always publicly visible as transactions. Receipts unaffected (the doc hash is already the record key). CONSEQUENCE: log-scan is now a legitimate indexing/incremental-sync option (closes the list-paging staleness gap described in indexer.py); eth_getLogs capped at 10,000 blocks per query (measured). OPEN with MANTRA: which events are guaranteed going forward — their own stated stance was still "no events" while their binary emits them (asked 2026-07-07).
- **Pagination facts (2026-07-07)**: records() silently IGNORES `pagination.reverse` in BOTH the list and keyed shapes, while registries() honors it (verified with identical tuple encoding — which also re-validates our encoder; MANTRA attributes the countTotal regression to their ML-313 registry-pagination fix). countTotal still 0 on every shape. Keyed reads return ONLY the latest version regardless of limit (limit=10 → 1 row). An explicit `index` DOES return that exact historical version (verified on dev-probe indexes 1–4) — history is per-index addressable, and the LATEST INDEX doubles as the VERSION COUNT, so version counting never needs countTotal.
- **83,541 was payload-specific, not a chain constant**: replicating the 2026-06-24 addRegistry estimate with the locked schema section-2 creation strings gives 79,163 (testnet) / 79,133 (mainnet); minimal strings ≈ 58k. The constant 30-gas cross-network offset is a state artifact, not a code difference. PARITY is the durable claim. The cost model is untouched — per-record figures came from the actual paid load.
- **OPEN QUESTION (product-relevant): EOA-only writes.** Per anchoring-abi.sol, write methods require an EOA caller → contract wallets (multisigs) cannot own or write registries as-is. Receipt registries are owned by filing parties (invariant 4); a firm wanting multisig custody would be blocked. Asked MANTRA 2026-07-07 whether this is permanent.
- **MANTRA metadata convention (proposed, application-level)**: `reason` / `correctedField` / `supersededBy` as shared change-annotation keys, core identity fields left application-defined (we pushed back on prescribing `citation`+`year` cores — `citation` duplicates the checksum, ~12M records of dead bytes). Adopting any of it, or any non-Active status, is a record-schema **v2 bump**; v1 (status always Active; supersede-by-new-version; `supersedes_reason`) stands as locked. docs/record-schema.md deliberately NOT edited: its section-5 rationale ("the method is absent") is obsolete but the policy is not, and the lock rule is version-bump-never-edit.
- **Corrections applied today**: anchoring.json (+updateRecordStatus, +revokeRole) with typed builders (`build_update_record_status`, `build_revoke_role`) in precompile.py; ABI goldens REGENERATED with the ethers 6.16.0 oracle (nvnm-tutorial node_modules, same as 2026-06-10) — ethers independently derives `0x97b40c25` / `0xacd58bc7`, so selector agreement now spans three independent sources (ethers, MANTRA's .sol, live dispatch); two new golden calldata cases; PUBLISHED_SELECTORS extended in test_abi_precompile.py. Suite: 490 passed (was 488). Also: precompile.py docstring, CLAUDE.md chain-constants block, IMPLEMENTATION_PLAN (exp (f) supersession note, gas-parity phrasing, W1 → RESOLVED), mantra-brief.md (dated corrigendum header — sent 2026-06-27, preserved as sent), proposal-mainnet.md (Appendix A row amendment). Ecosystem datum: registry count ≈ 1,978 on testnet — other teams actively use the module.

## 2026-07-15: Round-2 webapp flow revisions integrated (design bundle merged, not swapped)

The Claude Design round-2 bundle (`design_handoff_nvnm_cite_r2/`, from docs/design-prompt-r2.md; vendored in-repo) was integrated as a MERGE against production, not the drop-in its README claims. The round-1 lesson repeated, worse: despite being handed the shipped files, the designer diffed against its own round-1 universe. Its "no round-1 id was renamed or removed" claim is false — 17 production ids were missing (12 bound by app.js), including the Check replay affordances, the Verify tab's registry input, and the telemetry disclosure. Regressions rejected: "runs entirely in your browser / no network requests" privacy copy (contradicts item 0), precompile `0x…0808`, fictional RPC host, a global `receipts-v1` registry (violates invariant 4), dropped FAQ/how-it-works/eyecite attribution/ARIA wiring, and an invented, undocumented "KYA id" field (schema is locked; dropped). `firm-input`/`case-input` ids RETAINED — the handoff's `filer-input`/`matter-input` "new fields" rested on its false assumption they didn't exist.

ADOPTED (the actual round-2 design work, per HANDOFF-R2 contract delta): sticky `.tabs-bar` outside the header (stuck elevation, overflow fade cues, active-tab centering); verdict banner (bad/ok/warn, NOT_FOUND rows repeated inline, `data-print` suffixes); severity-ordered table with NOT COVERED behind a disclosure row (prints expanded) and summary chips as aria-pressed filters; the registry-line step — 4-step stepper, `#regline-callout` teaching the line BEFORE anchoring (item 3's discovery ordering, the review's P0 finding), `#regline-status` on prepare, post-anchor `rb-note` confirmation-never-instruction; wallet callout (MetaMask path / switch-network) + prepare gating + all stepper states wired incl. step 4 lifecycle; paste-provenance warning on Record; zero-citation empty state; source-text snippets on UNPARSEABLE/AMBIGUOUS; "as attributed in the brief" label; bundled Mata v. Avianca sample link; coverage honesty (server-filled `#coverage-scope`/`#legend-coverage`, About `#coverage-empty` fallback to live-status rows — the "loading forever" lie is gone); round-1 size meter now used (replaces the receipt-size hint line). Check busy is now an indeterminate progress bar; the determinate "citation 12 of 63" mode is wired client-side awaiting server progress events. CSS = production + the bundle's round-2 tail verbatim + masthead border moved to the tabs bar.

Engineering signals added: `verifier/check.py` `source_snippet()` (citations carry `snippet` for UNPARSEABLE/AMBIGUOUS); `webapp/service.py` prepare returns `registry_line_found` (re-extracts the exact uploaded bytes, case-insensitive search for the registry name — a warning, never a blocker); `static/sample-mata-avianca.txt` (copy of the committed RECAP fixture) served for the sample run. Registry-line format settled: `Citation verifications: NVNM Chain registry <firm>--<case>` (same string the post-anchor banner has used since 4.5).

Verified: 490 tests green; mechanical contract check (every app.js-referenced id exists in the merged index.html; every JS-built class exists in app.css); live testnet smoke — sample run renders the red verdict with both Mata fabrications (925 F.3d 1339, 516 F.3d 1237), filter/disclosure row counts 13→2→27, prepare API returns registry_line_found false/true correctly against registry 745 (write probe: unauthorized for a non-owner wallet, as designed), sticky/fade/scroll behaviors confirmed at 375px, zero console errors. The bundle's raw zip is gitignored; the extracted bundle is committed as the design-of-record reference.

## 2026-07-16: Mainnet bulk-load export generated — full US case-law scope, one file per registry

Albert's go: generate the full per-registry record files for the blockchain-team bulk upload (the handoff the 2026-07-15 sample README promised). Deliverable: `data/mainnet-full-export/` (+ `data/nvnm-cite-mainnet-full-export.zip`, 437 MB) — gitignored, local only; hand over out-of-band.

- **Scope rule v2 (the mainnet load set):** a citation row is registry-eligible iff CL type ∈ {1 federal, 2 state, 3 state_regional, 5 scotus_early, 8 neutral} (vendor cites 4/6/7 stay out, pilot rule) AND the reporter string is a reporters-db edition. This SUPERSEDES the pilot per-court reporter whitelists (`loader/scope.py`) for the mainnet set only — the testnet registries and the daily updater keep the pilot whitelists. Consequence: first-series `F.` and the early SCOTUS nominatives (Dall., Cranch, Wheat., Pet., How., Black, Wall.) are now IN (real, citable, reporters-db-known; excluding them at production scope would manufacture false NOT_FOUNDs).
- **Fresh snapshot census (2026-06-30 bulk, all courts):** 10,070,727 clusters total, 7,844,573 with ≥1 citation; 18,123,725 citation rows; **11,962,952 distinct eligible (court, key)**; **2,114 courts** with ≥1 eligible key (83 more have clusters but no eligible citation). Tracks the 2026-06-24 census (~7.84M / ~11.91M) plus one quarter's growth + the type-5 additions. Varghese `925 F.3d 1339`: 0 rows, still absent.
- **Export:** 11,944,960 records across 2,114 registry files, 3.60 GB raw / 476 MB gzipped. Submit shape: the six writer fields only, rendered through `loader/records.py` (same collision/truncation code as the testnet load). Tranches: **T1 federal appellate 14 registries / 1,028,222** (scotus 226,555; ca11 42,176 vs 41,988 on testnet), **T2 federal complete 305 / 545,922**, **T3 state pilot (CA/NY/TX/FL/IL) 567 / 3,617,459**, **T4 state remainder 1,228 / 6,753,357**. Collision-form keys: 1,033,947 (8.7%; the testnet set's ~50% was SCOTUS-skew, not the corpus norm). Largest registry: us-nyappdiv 1,209,174.
- **T1 vs the plan's 924,421:** +103,801 = the type-rule additions (early scotus nominatives, first-series F.) + one quarter of new opinions. The whitelist figure was method-bound, not a target.
- **Exclusions (never silent, never invented):** 17,992 keys / 17 reporter strings, counted per registry in the manifest. 92% is `P.R.` (16,525 — CL's Puerto Rico official-reporter string is not a reporters-db edition); the rest are CL typography variants (`Fed. Appx.` 546, curly-quote `F. App’x` 142, `F. 4th` 62) + obscure historical reporters. Disposition: deferred to the Phase 7.7 normalizer rebuild (variant mapping is a normalizer-spec question; a cite goes on chain only from an authoritative source under a known edition).
- **Validation (all passed):** full-file pass over every record — sha256 vs manifest, six-field shape, caps (checksum ≤64 B, uri/metadata ≤2048 B), non-empty fields, per-file line counts; **records + exclusions = census keys exactly** (11,944,960 + 17,992 + 0 = 11,962,952). Live testnet keyed `records()` read for `410 U.S. 113`: all five content fields byte-identical to the export line. All 606 isLatest scotus rows from the 2026-07-15 chain-read sample: identical in the export, 0 diffs. Determinism pinned by test (same corpus → same sha256s; gzip mtime=0).
- **Registry ownership (recommendation, stated in the handoff README):** registries created by the Inveniam-held mainnet key from the key ceremony (creator = admin, permanently); loader keys granted editor via `grantRole`, revocable after the load. Pending Albert/ceremony sign-off; the README states it as a requirement of the load plan.
- **Code:** `loader/courtlistener.py --courts all` (new pass 0 collects cluster-referenced docket ids — 9,765,364 of 70.7M dockets — so all-courts fits in memory); new `loader/export.py` (scope-v2 filter, tranche layout via courts-db `system`/`location`, manifest with per-file sha256s, README rendered from the manifest so figures cannot drift); `creation_strings` moved from the phase-2 script into `loader/records.py` (single source, byte-identical output). Corpus build: 26 min wall; export render: 4 min; 0 malformed rows in any of the three 2026-06-30 files. Suite: 494 green (4 new tests).
- Tranche classification note: courts absent from courts-db land visibly in tranche 4 with `in_courts_db: false` in manifest + registries.json (none required improvised creation strings this run — all 2,114 exporting courts resolved).

## 2026-07-16 (addendum): load mechanism = MANTRA chain software upgrade, not tx-based bulk load

Albert: MANTRA will load the mainnet record set via a chain software upgrade (direct state migration), superseding the tx-based editor-key load plan for the INITIAL load only. Consequences, folded into the handoff README (regenerated + re-zipped):
- The ~$55k gas figure and the parallel-key wall-clock model are MOOT for the initial load (they still price the alternative and any re-load). The daily updater still writes normal addRecord txs afterward.
- **Registry admin assignment becomes the critical handoff item**: creator = admin is permanent and the migration sets it directly, so every registry's creator must be the Inveniam-designated mainnet address (from the key ceremony, to be provided before the upgrade is cut). Wrong creator = 2,114 registries frozen at the snapshot. This preserves invariant 4 the same way addRegistry would have.
- Migration must reproduce addRecord field semantics (recordId sequential per registry, index=1, isLatest=true); file order is deterministic so recordId assignment is reproducible.
- Migrated records have no originating txs and no AddRecord events — log-scan indexers cannot see the initial load (asked MANTRA to say if they'll synthesize events). Our verification is unaffected: keyed/paged records() reads.
- **Acceptance = reconcile-zero**: after the upgrade we reconcile every registry against the exported files from a public node until the diff is zero or documented. The export files + manifest sha256s are unchanged and remain the contract.

## 2026-07-31 — Mainnet transition + anchoring v1.2.0 migration (id-keyed interface, non-unique names)

**Context.** MANTRA's 2026-07-30 chain upgrade loaded the full-scope corpus onto mainnet by state migration (2,114 court registries / 11.94M records) and shipped anchoring module v1.2.0 — a breaking interface change. This session migrated the codebase and made the webapp the production surface on mainnet. Everything below was verified LIVE (read-only) on both networks on 2026-07-31 before any code was written.

**v1.2.0 interface facts (live-verified, both networks).**
- Registry names are NON-UNIQUE; the numeric `registryId` is the canonical reference for reads AND writes. The old name-keyed selectors return `unknown method id` on BOTH networks — the pre-migration code was broken everywhere, so this migration was mandatory, not a mainnet option.
- New selectors (computed with our keccak from the v1.2.0 doc, confirmed live): `records(uint64,string,uint64,uint64,Page)`=c7be5e37, `registries(uint64,Page)`=17bd3e65, `addRecord((uri,checksum,checksumAlgo,metadata,timestamp,status,recordId,index,isLatest,registryId))`=64d25295. Unchanged: addRegistry=318b38b1, grantRole=b8fdd1a7, revokeRole=acd58bc7, updateRecordStatus=97b40c25. `registries()` lost its name filter entirely: name/owner search = full enumeration (registryId=0, offset-paged, 200/page).
- Record struct: registry name string removed; `registryId uint64` appended. Registry struct unchanged. Keyed misses still ERROR with `collections: not found` (key format now e.g. `'("82", "999 U.S. 999")'`). Paging contract (200-row cap, short-page termination) re-verified under the new selectors.
- Mainnet corpus: 2,182 registries total; OURS are exactly ids 69–2182 contiguous (69=us-ca1 … 2182=us-wyo; us-scotus=82, us-ca11=71, us-ca2=72), all creator nvnm14a3em3mr9mvta9ccgk80wn0dxgzt5lkt2r8trx, createdAt 2026-07-30. Roe spot-check: records(82,"410 U.S. 113") → recordId 138702, cite-canonical-v1, Active. State-migrated rows emitted NO events (per our handoff spec) — offset-paged records() is the ONLY corpus rebuild path. Testnet (also on v1.2.0): us-scotus=737, us-ca11=738, dev-probe=733, inveniam--mata-v-avianca=745.

**KEY FINDING — the mainnet admin key is the .env dev key.** `NVNM_TESTNET_KEY` derives to 0xaf639Dc7632eD8BE9718458EF74Ded3204bA7EcB = nvnm14a3em3… — the sole admin of all 2,114 mainnet registries. The old CLAUDE.md claim "mainnet keys are not session-accessible" is FALSE as of the mainnet load. Decision (Albert): guard + flag only this session. Code guard: `config.signing_context` is the single signing gate — mainnet signing refuses unless BOTH `NVNM_MAINNET_WRITE_OK=1` AND a distinct `NVNM_MAINNET_KEY` are set (never in .env, never in a session); every signing call site routes through it and pre-flights `rpc.chain_id()`. RECOMMENDED TEAM OPS (outside sessions): key ceremony for a fresh admin, then grantRole(new, "admin") + revokeRole(old, "admin") across the 2,114 registries (~4,230 txs ≈ ~35 min at the measured 2.1 tx/s), or MANTRA-side reassignment. Until then, treat .env as a production secret.

**Registry manifests — the name→id trust anchor.** With names forgeable, published registry IDs are the trust root (this was already the plan's mainnet-cutover posture; v1.2.0 makes it load-bearing). New artifacts checked in as package data: `src/nvnm_cite/chain/registry_manifest_{mainnet,testnet}.json` (schema `nvnm-cite-registry-manifest/v1`), generated READ-ONLY by `scripts/build_registry_manifest.py`, which enumerates registries(), filters creator == the official attestor AND name ∈ the bulk-load export, then hard-verifies (mainnet: exactly 2,114, ids contiguous 69–2182, every export name present once, zero same-name duplicates within the creator set, Roe recordId spot-check) and aborts loudly otherwise. Runtime resolution goes through the pinned manifest (`chain/registrymap.py`), never a live name search; `nvnm-cite manifest-verify` re-enumerates and diffs (drift = regenerate + review + commit, never silent). Both manifests generated at blocks 2,070,234 (mainnet) / 2,897,390 (testnet); manifest-verify clean.

**Coverage = the full manifest (Albert's call, accepting state-mapping risk).** `COVERED_REGISTRIES` (the pilot pair) is replaced by the per-network manifest key set — mainnet 2,114, testnet the pilot two. Mitigation for the one dangerous failure mode (false NOT_FOUND on state cites, where the 7.7 normalizer rebuild hasn't proven the mapping): NOT_FOUND results outside the proven FEDERAL_APPELLATE set (SCOTUS + 13 circuits) carry `confidence: "expanded-coverage"` plus a caution ("a flag to verify, never proof of fabrication; never delete a citation on this signal alone") rendered by CLI and webapp. Live sanity: Palsgraf (248 N.Y. 339 → us-ny #536) and Tarasoff (17 Cal. 3d 425 → us-cal #388) VERIFY against mainnet; invented state cites come back NOT_FOUND with the caution. Follow-ups deferred: 7.7 state golden suite; telemetry watch on state hit rates.

**Receipts under non-unique names (invariant 4 amendment).** The discovery link now carries the numeric #id — locked line format (`receipts/anchor.py::registry_line`): `Citation verifications: NVNM Chain (chain <id>) registry #<id> — <firm>--<case>`. A NEW registry's id exists only after addRegistry confirms, so anchoring into a new registry is TWO steps: create → recover the id from the AddRegistry event in the tx receipt (`precompile.decode_add_registry_log`) → build/send the id-keyed addRecord. CLI `anchor` does this sequentially in one command; the webapp does setup-tx → /api/tx (now decodes precompile events) → re-prepare with X-Registry-Id → anchor. Existing-registry resolution = enumeration by creator+name; MULTIPLE same-name matches are always surfaced for a human pick (CLI: exit with the list, require --registry-id; webapp: candidate picker), never first-row. Receipt v1 schema: NO bump ({id,name,head_block} already had the id; chain_id already a field — its default was removed, it now comes from the active network). Two additive doc-level changes (record-schema §7): `registries` = the registries actually READ for the document (not the coverage set), with deterministic truncation into optional `registries_omitted` if a many-court brief would breach the 2048 B cap. `receipts/verify.py` is id-keyed and skips the recompute with an explanatory note when the receipt's chain_id ≠ the active network. Legacy name lookups (pilot-era links) still work in the webapp via a cached-enumeration fallback with ambiguity surfacing.

**Network profiles + defaults.** `config.Network` (mainnet/testnet: chain id, cosmos id, RPC + env override, explorer, gas token); `get_network()` = arg > NVNM_NETWORK > caller default. READS default mainnet (check/verify/stats/manifest-verify, webapp); WRITES default testnet (anchor, loader). The webapp serves its network block via /api/status (chain params, explorer, gas token, live gas price) — app.js hardcodes nothing chain-specific and gates wallet actions until it loads.

**Chain index schema v2.** `chain_index.sqlite` keys on registry_id (name denormalized for display); `meta.index_schema=2`; a v1 file is REFUSED with a delete-and-rebuild message (rebuildable cache — no migration code; it never recorded ids). localindex treats v1 files as absent. Loader/update/reconcile migrated (testnet write tooling); scripts/phase0_* + phase2_create_registries annotated as pre-v1.2.0 historical.

**Tests.** 528 passed + 5 opt-in mainnet-smoke (NVNM_MAINNET_SMOKE=1, strictly read-only vs chain 1611: registry 82 facts, Roe record, keyed-miss marker, range sentinels, paging cap). Golden ABI vectors regenerated with ethers 6.16.0 from the new vendored ABI (+event topics + an AddRegistry log-data vector); all seven selectors pinned. Fixture ids = testnet (737/738) wherever flows are simulated; mainnet ids only in manifest tests + smoke. New suites: bech32 (chain/bech32.py, BIP-173, pinned to the admin address pair), registrymap load/drift, signing-guard refusal, two-step send with event recovery, ambiguity surfacing, expanded-coverage marker, receipt truncation. Live gates run green: testnet check round-trip (id-keyed), mainnet read-only CLI check + browser smoke (Roe VERIFIED, Varghese NOT_FOUND from the production corpus), manifest-verify clean on both networks.

**Webapp 0.2.0.** New: GET /api/receipt/registries (creator listing via bech32 conversion), X-Registry-Id on prepare, /api/tx event decoding + assigned-id surfacing, verify-by-#id with pasted-line parsing + legacy-name fallback, "My registries" picker, expanded-coverage caution rendering, server-fed net badge/footer/gas copy (wmmUSD on mainnet), registry-line tiers (id / name-only / none). One real-receipt UI element intentionally NOT exercised this session: the two-tx live anchor needs a browser wallet + explicit OK (pending below).

**Open / pending.**
- LIVE two-tx receipt anchor on testnet through the new flow (create + anchor): pending Albert's explicit OK (chain-write policy). The same code path is proven hermetically (event-recovery send tests) and the prepare/estimate halves live.
- Admin-key rotation (team ops, above). CLAUDE.md corrected meanwhile.
- Full mainnet reconcile of the 11.94M records vs the export files (the handoff acceptance) — separate ops effort; `manifest-verify` + smoke sentinels cover the registry layer only.
- MCP server (Phase 5) unchanged; it will inherit the id-keyed core.

## 2026-08-01 — Normalizer 1.1.0: corpus-wide mapping audit + expanded jurisdiction mapping

**Trigger.** Albert: fix the A.D./T.C. gaps and "check every other mapping and normalization mechanism." Method: the chain corpus as ground truth — scanned all 11,944,960 export records (byte-validated vs chain), built an edition→registries histogram + one exemplar per (registry, edition) pair (10,423 pairs, 842 editions), rendered each exemplar as a realistic citation (with a courts-db parenthetical, and bare) and demanded normalize() round-trip to the same (registry, canonical).

**Results (record-weighted reachability of the corpus).** With parenthetical: 70.01% → **96.84%** ok. Bare: 21.01% → **38.59%** ok. Wrong-registry: 0.03% — inspected: ALL parallel-key artifacts (e.g. an S. Ct. cert-denial key stored under a circuit registry maps to us-scotus, which is jurisdictionally correct), zero mapper errors. Remainder decomposes into honest AMBIGUOUS (1.74%: genuinely multi-court forms — N.Y.2d splits 65/35 ny/nyappdiv in the corpus itself, Misc.*, M.J., Ky. era courts), not-extracted 0.76% (audit rendering artifacts like volume-first "1976 T.C. Memo. 300" which no lawyer writes — the real form "T.C. Memo. 1976-300" parses and maps, golden-pinned — plus volume-0 corpus noise), vendor 0.63%.

**New mechanisms (jurisdiction.py; spec §4 amended, mapping rules now 1–6).**
- Rule 2 hardened: eyecite's metadata.court is CORROBORATION-CHECKED when it contradicts the citation's own reporter default — measured overreach: in a string cite, eyecite attached the NEXT citation's "(N.Y. App. Div. 1912)" to "54 Cal. 3d 868" (court='nyappdiv'); the reporter default now wins unless the adjacent parenthetical confirms the claimed court. Caught live before ship.
- Rule 4 (new): general court-parenthetical fallback by exact longest-prefix match against courts-db citation_strings (measured globally unique, 1,959/1,959). Appellate-Division forms ("App. Div.", "1st–4th Dep't") are STATE-GATED on the reporter family: N.Y. editions → us-nyappdiv, N.J. editions → us-njsuperctappdiv, otherwise refused — the first draft mapped bare "App. Div." to NY and mis-routed "360 N.J. Super. 360 (App. Div. 2003)"; caught live before ship.
- Rule 5 (new): reporter-edition inference from `normalizer/reporter_registries.json` (356 editions; generated by scripts/build_reporter_map.py, reviewed + committed like the registry manifest). Inclusion guards: ≥100 records, ≥99.5% single-registry dominance (strict uniqueness fails on CL attribution noise — A.D.2d has 177,540 nyappdiv records and 8 strays across four other registries), exactly one reporters-db reporter per edition (excludes shared nominatives like Cranch), non-vendor, manifest-member registry. Curated: EXCLUDED M.J. (spans CAAF + four service courts; corpus holds only CAAF) and Fla. L. Weekly Supp. (multi-trial-court weekly); ADDED bound T.C. → us-tax (corpus stores only slip/memo forms). This resolves bare A.D./A.D.2d/A.D.3d, N.Y., Cal., Ill., Ga., state officials generally, neutral formats (2019 OK 5, IL App (1st)), nominatives (Barb., Vroom, B. Mon., …), T.C. No./T.C. Memo.
- VENDOR disposition (new, normalizer-level; verifier maps it to NOT_COVERED with the specific reason, NO chain read): Westlaw + generic/federal LEXIS have ZERO corpus presence, so a lookup can only manufacture a false "not found" — vendor-disposed even with a parenthetical. Court-specific LEXIS editions are 3,279,710 real parallel keys ON CHAIN (27% of the corpus!) across 92 editions — they map and verify like ordinary reporters (dominance-refused ones, e.g. N.Y. App. Div. LEXIS with a genuine 4,550-record us-nysupct second population, stay VENDOR bare but map via an explicit parenthetical). The artifact records `lexis_editions_present` for this split.

**Versioning.** NORMALIZER_VERSION 1.0.0 → 1.1.0. Spec §7 clarified: mapping changes bump the normalizer version only; `checksumAlgo` (cite-canonical-v1) is reserved for KEY-affecting changes — keys are untouched. Old receipts recompute under 1.1.0 with the existing version-difference note.

**Tests.** Goldens 214 → 232 vectors (ny_appdiv, tax_court, vendor_cites incl. the NJ/NY gating and LEXIS split, reporter_inference, parenthetical_index incl. precedence, still_ambiguous incl. M.J./S.W.3d/bare F.3d). Suite 550 passed incl. live testnet. Live mainnet spot-check verifies: Podraza (A.D.2d), Palsgraf (bare N.Y.), Christensen (the overreach fix), a 1912 N.Y. App. Div. LEXIS parallel key, WL → vendor NOT_COVERED, and Mata's mis-cited "360 N.J. Super. 360" now routed to the RIGHT registry before missing honestly.

**Corpus-quality findings (reported, not fixed — corpus ops out of session scope per Albert 2026-07-31):** LEXIS parallel keys are 27% of the corpus despite the handoff doc's vendor-exclusion claim; us-afcca holds exactly ONE record, keyed "54 N.J. 783" where the reporter is almost certainly M.J.; volume-0 "Fulton County D. Rep." rows in the Georgia registries; "N.Y. App. Div. LEXIS" keys split nyappdiv/nysupct; Texas appellate records split between us-texapp and per-district us-txctappN registries (granularity inconsistency).

## 2026-08-02 — Agent-facing web surface: the site teaches agents; NO nvnm-cite MCP server

**Trigger.** Albert: evaluate how a human user's AI agent (Claude Desktop / ChatGPT Desktop) could use the tool, then make it usable — with two constraints set explicitly: no new MCP server for nvnm-cite, and no modifications to the generic NVNM Chain MCP server for this use case. nvnmcite.com has been purchased; devops will stand up the app there. The goal: agents navigating to the site should find a tutorial for the available endpoints.

**Decision: the website is the agent surface.** Four artifacts now ship inside `src/nvnm_cite/webapp/static/` and serve at the site root on any deployment:
- `llms.txt` — root discovery file per the llms.txt convention (what the service is, what an answer asserts, one line per endpoint, links to the two docs below).
- `agents.md` — task-oriented tutorial: conventions (raw-bytes POSTs, Content-Length required, uniform error shape, 502-is-never-a-verdict), curl-worked workflows for check / verify-by-hash / tx inspection / the two-step anchor flow, the five-status semantics reference incl. the expanded-coverage caution and name_check, privacy model, FLP attribution.
- `openapi.json` — OpenAPI 3.1 contract for all six endpoints, schemas written from the actual response-building code (Citation fields, lookup proof replay, prepare variants ready/setup/ambiguous, unsigned-tx + write-probe shapes). **Publishing this freezes the current API shape (raw-bytes body + X-* headers, URL-encoded values) as the public v0.2 contract**; a later break must be versioned.
- `robots.txt` — permissive, comment pointing agents at /llms.txt.
The ONLY server change: `.json` and `.md` added to the static-type whitelist in server.py. Copy is network-neutral — agents are told to read `/api/status` first for chain identity, so the same files are correct on testnet, localhost, and mainnet deployments. +1 test (content types + OpenAPI paths match the real surface); suite 551 passed + 5 opt-in smoke.

**Decision: `GET /api/cite?q=` (single-citation check endpoint) REJECTED.** Considered to give fetch-only agents (GET-only web tools) direct access to the core question; rejected for: (1) citations enter URL space, so the privacy posture stops being a code property and becomes a forever-maintained log/CDN scrubbing discipline at every infra layer (repeated queries from one client reconstruct a brief's citation set); (2) context-free verdicts would systematically diverge from document verdicts (no short-form resolution, no parenthetical/jurisdiction context → more AMBIGUOUS, occasional different answers) — two surfaces disagreeing about "the same" citation is a trust problem; (3) a free GET fanning out to live RPC reads invites scripted load and pollutes the opt-in by-citation telemetry. A JSON-body POST variant remains the fallback if single-cite agent access is ever wanted. Consequence accepted: fetch-only chat agents can read the docs and use the GET endpoints (receipt lookup, tx, status) but cannot run document checks; shell-capable agents, custom GPT Actions (which consume openapi.json directly), and browser-driving agents get everything.

**Invariant clarification (Albert).** NVNM Cite and NVNM Chain never assert good-law status or that a case supports a proposition — but a USER'S OWN AGENT may use NVNM Cite outputs to make exactly such determinations under its own mandate; that is the consumer's business, not ours to prevent. Agent-facing copy therefore states precisely what an answer asserts (existence of a registry record at a stated block) and explicitly leaves interpretation to the consumer, rather than policing downstream use. Invariant 2's scope is the system's own assertions, unchanged.

**Deployment notes for the nvnmcite.com stand-up (devops).** The four files serve from the app itself — nothing extra to deploy. A CDN/proxy in front must not override the app's `Cache-Control: no-store`. `openapi.json`'s servers block assumes the app is mounted at the domain root. `/api/status` becomes public information (it reports RPC URL, index state, telemetry flag) — devops should glance and confirm. CORS stays closed: custom-GPT Actions and web-fetch tools call server-to-server, and browser-driving agents are same-origin; no cross-origin browser caller exists. Optional politeness gap: the stdlib server 501s HEAD requests; crawlers fall back to GET.

**Observation (reported, not acted on — out of scope by Albert's constraint):** the generic NVNM Chain MCP server's `nvnm_overview` describes the chain purely as a hash notary ("stores only one-way hash fingerprints … never the documents") — factually wrong for the 11.94M plaintext citation records — and its canonical journey steers agents toward raw registry writes with none of the trust boundary (no normalizer, no manifest name→id anchor, no keyed-miss semantics). Worth flagging to that team; nothing changed here.

## 2026-08-02 — Public-release prep: history verified clean, stale files dropped; repo found ALREADY PUBLIC

**Trigger.** Albert: get the GitHub files (and local stragglers) ready for public deployment; remove files no longer relevant.

**Verified before touching anything.** Full-history secret scan across all 78 commits, comparing the actual `.env` values against every blob WITHOUT printing them: `.env` was never tracked in any commit; `NVNM_TESTNET_KEY` (= the mainnet admin key until rotation) and `COURTLISTENER_TOKEN` appear nowhere in history; `.env.example` carries placeholders only (its one real value is the public testnet RPC URL). The golden signer/secp256k1 vectors use synthetic spec-example keys (0x4646… is the EIP-155 worked example), not real ones. `.gitignore` correctly excludes `.env`, `*.sqlite*`, and `data/` (11 GB local, incl. the mainnet export — untouched).

**FINDING: the repo is ALREADY PUBLIC** (`gh repo view`: visibility PUBLIC). Consequences: (1) the full history — including every state-file revision — is already published, so tree cleanup is presentation, not secrecy, and no history rewrite was attempted (public + cloneable makes rewriting moot and harmful); (2) the state files' admin-key-location disclosure (the `.env` dev key is sole admin of all 2,114 mainnet registries, on a dev machine) is public knowledge — no key material leaked, but ROTATION IS NOW URGENT (team ops); scrubbing text would not help and the operative warning stays in CLAUDE.md; (3) with no LICENSE file, the public repo is all-rights-reserved by default — the licensing decision (task 6.4, human-gated) is now live, along with the empty GitHub repo description.

**Removed from the tree** (git history + phase-N-done tags preserve everything; all four scripts were self-annotated "pre-v1.2.0 … non-functional against current chains"): `scripts/phase0_experiments.py`, `scripts/phase0_roundtrip.py`, `scripts/phase2_create_registries.py`, `scripts/backfill_supplemental.py` + `scripts/supplemental_citations.csv`, and the merged `design_handoff_nvnm_cite_r2/` bundle (7 files; integrated 2026-07-15). Local untracked `NVNM Cite.zip` deleted; its gitignore entry removed.

**Archived to `docs/ARCHIVE/`**: `anchoring-abi.sol` → `anchoring-abi-pre-v1.2.0.sol`, `design-handoff.md`, `design-prompt-r2.md`, `mantra-brief.md`, `proposal-mainnet.md` (completed proposals; mainnet is live). Kept live in `docs/`: the two specs, `web-demo.md`, `brief-for-firms.md`.

**Reference/metadata updates.** CLAUDE.md ABI pointer → the ARCHIVE path; app.js header comment no longer names the deleted bundle directory; web-demo.md design-handoff link → ARCHIVE; README Layout section rewritten (the "5 methods" ABI claim was stale; now lists the current tree incl. the agent-facing static docs). Version aligned: package `__version__` and pyproject 0.1.0 → 0.2.0 (matching the webapp), plus `readme` and `[project.urls]` (Homepage nvnmcite.com, Repository) in pyproject. Suite green after every step: 551 passed + 5 opt-in smoke.

**Open (Albert / team).** LICENSE choice and repo description; admin-key rotation (escalated above); devops nvnmcite.com stand-up per the 2026-08-02 agent-surface entry.

## 2026-08-02 — Correction: mainnet admin key ROTATED; `.env` is a testnet-only secret again

Albert (same day as the release-prep entry, which had escalated rotation to URGENT): the mainnet admin authority was rotated — the old dev-key address (`0xaf639dc7…` / `nvnm14a3em3…`) no longer holds admin over the 2,114 mainnet court registries, and no session key is needed for anything anymore (no writes remain on our side: the corpus was MANTRA-state-migrated, and receipts are signed by the filing parties' own wallets). Consequences: `NVNM_TESTNET_KEY` in `.env` is a testnet-only dev secret, not a production credential; the URGENT flag in the entry above is resolved; CLAUDE.md's 2026-07-31 KEY FINDING rewritten as resolved-historical. No code or manifest impact: the registries' immutable creator FIELD still reads the old address forever — authority and creatorship are different things, and `manifest-verify` checks names/ids/creator, not roles.

Verification note (evidence discipline): the session attempted a read-only chain probe (estimateGas / eth_call of a dummy `addRecord` into registry 82 with `from` = the old address; deny-by-default writes make a clean estimate an authority oracle) — the session sandbox blocked simulating calls from that address, so this correction is recorded on Albert's word, not chain-verified in-session. The one-command re-check (curl eth_estimateGas against the public mainnet RPC) is in the 2026-08-02 session log; expected result post-rotation: an unauthorized error.

## 2026-08-02 — Real-filings corpus run: normalizer 1.2.0 + authority grouping; SCOTUS parallel-cite gap OPEN

**Trigger.** Albert supplied eight real public filings for testing and webapp improvement: both Mata v. Avianca documents (the fabricated-citations reply brief, ECF 24, and Judge Castel's sanctions opinion, ECF 54), the Walters v. OpenAI CA11 brief, the Dobbs / SFFA / 303 Creative merits briefs, a California amicus, and an Ohio Power Siting Board merit brief (`data/public-legal-filings-pdf/`, gitignored). A ninth scanned document was skipped — **DECISION (Albert): scanned documents are permanently out of scope; OCR will never be supported.**

**Baseline run (1.1.0, live mainnet).** End-to-end green, 1.8–14 s per document (99-page SFFA brief: 13.3 s, 343 occurrences). The product works on the real thing: every Mata fabrication NOT_FOUND (925 F.3d 1339, 516 F.3d 1237, 174 F.3d 366, 284 F.3d 353, the IL App and N.J. Super. fakes), the sanctions opinion's quoted copies too, WL cites honestly NOT_COVERED, and name_check caught a real us-dcd cite carrying the fake opinion's invented case name. The CA amicus is genuinely case-light (its few Cal.5th/U.S. cites verified; recall confirmed against the extracted text). But real briefs exposed heavy noise: 88 UNPARSEABLE rows corpus-wide (stranded shorts like "539 U.S. at 331" ×15 in SFFA — eyecite resolves shorts by antecedent NAME and strands the rest; orphan Id./supra tokens; §-fragments of statute/CFR cites, 34 in the Ohio brief), 28 AMBIGUOUS (21 in the Ohio brief alone: Ohio style cites three parallel reporters per case, each scored separately), and one true mapping bug: eyecite claims the D.C. court 'supctdc' from "(Sup. Ct. 2004)" after a N.Y. Misc. 3d cite, and rule 2 trusted any claim with no reporter-table default to contradict it.

**Shipped: normalizer 1.2.0** (KEY format unchanged; spec §§3/4/5 amended; adjudications here). (a) Orphan short-form fallback: a stranded short attaches to the same-volume/edition full cite whose first page is the largest ≤ the pin page (Gratz 244 / Grutter 306: "at 331"→Grutter, "at 270"→Gratz; no candidate → stays unresolved; measured: with a SINGLE same-volume antecedent eyecite resolves natively with no page check, so the window governs only the fallback tier). (b) Law-section tokens (^[([]?§) get the new OUT_OF_SCOPE disposition. (c) State-consistency gate: a rule-2/rule-4 court whose courts-db location-state contradicts the reporter's reporters-db mlz_jurisdiction state set is refused; inert when either side is unknown (federal/national reporters, federal/foreign/territorial courts, incl. the courts-db 'Washignton D.C.' typo, all mapped); kills the supctdc bug — Misc. 3d honestly AMBIGUOUS. (d) Star pagination (*4) accepted as pin material before a corroborating parenthetical.

**Shipped: verifier authority grouping + accounting** (check.py). Parallel-citation clustering: consecutive full cites whose gap is only commas/spaces plus at most one bare pin page (≤16 chars — "58 Ohio St.2d 108, 110, 388 N.E.2d 1370"), with punctuation-insensitively matching back-propagated party names (measured: eyecite renders the same defendant "L.L.C." from a reporter cite and "LLC" from the neutral cite in one run), union-find into ONE authority row: strongest member by STATUS_ORDER presents, others stay visible under `parallels`; by_status counts authorities. Unresolved Id./supra fold into `unresolved_references` (count + per-form snippets), §-tokens into `summary.law_sections_out_of_scope` — accounted, never silently dropped, no longer scary rows. us-scotus NOT_FOUND reasons carry the parallel-reporter caveat (below). Snippets/gap checks now index the CLEANED text (spans were always cleaned-text offsets). Webapp renders parallels + two accounting chips; CLI table renders parallel sub-rows + summary lines; openapi.json/agents.md document the additive fields.

**Measured effect (same eight filings, 1.1.0 → 1.2.0).** UNPARSEABLE 88 → 3, AMBIGUOUS 28 → 9, NOT_FOUND 23 → 23 (unchanged, as it must be), VERIFIED 252 → 250 (two verified rows became verified parallels nested under a verified primary). Ohio brief: 21 ambiguous → 1 (the survivor has a PDF page number spliced mid-cite: "2014-Ohio- 6 3764"). The three surviving UNPARSEABLE are correct refusals: two short-form-only citations whose full cite never appears in the document (Telescope Media 936 F.3d), and the SFFA brief's own typo ("575 U.S. at 375" for Fisher II, which is 579 U.S.). Remaining ambiguous: the SCOTUS "opinions below" front-matter pattern (court named in prose, no parenthetical — candidate future rule) and by-design splits. Tests 562 (+parallel/pin-gap/semicolon/§/scotus-note verifier tests; three stale orphan-Id. assertions updated); goldens 242 vectors / 269 rows (+short_fallback, law_sections, state_gate). Live-mainnet browser verification: Buckeye Wind VERIFIED in us-ohio with '2012 Ohio 878' + '966 N.E.2d 869' as nested parallels, Roe's '93 S. Ct. 705' parallel independently VERIFIED on chain, both new chips rendered (the Browser-pane screenshot capture faulted — known harness issue — verification via the rendered DOM text).

**FINDING — corpus gaps (team ops; not code):** real, prominent cases miss because the source data carries only one of the parallel cites. Census-verified: Hobby Lobby / McCullen / SBA List merits clusters have `134 S. Ct.` but NO official `573 U.S.` cite; Fulton has only `593 U.S. 522` (no S. Ct.); Brush & Nib's Ariz. Supreme Court opinion (448 P.3d 890) and Otto's 6 F.4th 1247 are absent outright (CL holds only the related lower/prior opinions).

**OPEN DECISION (pending, next phase): the SCOTUS parallel-cite fix.** Census classification of scotus clusters: 294,712 carry both (verify either way; no help needed), 160,327 official-only, 43,925 S.Ct-only (since-2000: 58,359 / 41,565). Key fact: a crosswalk is NOT derivable from local data — the problem rows are exactly the ones CL lacks — so external sourcing (Caselaw Access Project / the Court's own volume tables) is required under every option. Options: **(a)** on-chain backfill of the missing parallel keys — durable, keeps the chain the single lookup authority, fixes every consumer (third-party verifiers, direct chain readers); ~$940 at measured gas for all 204k, tens of dollars scoped to merits opinions; a team write under the post-rotation admin. **(b)** vendored client-side crosswalk fallback in the verifier — fast, consistent with the manifest / reporter-inference-table precedent (versioned, published, reviewed); the verdict would disclose "verified via the parallel key" with both queries in the replay block; only our own verifier benefits. **(c)** both: ship (b) now, (a) retires it. Recommendation on record: (c); the census can generate the exact sourcing target list (cluster, case name, missing cite) on demand. Interim mitigation shipped: the us-scotus NOT_FOUND caveat.

## 2026-08-04 — Repo transferred to the NVNM-Chain org

**Trigger.** DevOps k8s standardization push: apps deploy from the official GitHub orgs. Albert asked to move nvnm-cite in.

**What happened.** GitHub repo transfer `aberdellans/nvnm-cite` → `NVNM-Chain/nvnm-cite` (full history, tags, and the repo itself moved; GitHub 301-redirects the old URL permanently unless the old name is ever reused). Precondition: an org owner deleted the stale `NVNM-Chain/nvnm-cite` fork (created 2026-06-28, no shared SHAs with current main after the public-hygiene history rewrite, nothing unique of value; its history still tracked `.claude/launch.json` with a local absolute path). Supersedes the 2026-06-13 note "Repo: github.com/aberdellans/nvnm-cite, private; can transfer to an org later if needed" — that line stays as history.

**Local/metadata consequences.** `origin` remote re-pointed at the org URL; pyproject `[project.urls]` Repository updated. No code, schema, or test impact.

**Open (Albert / team).** Albert landed with write-but-not-admin on the transferred repo (org default role): settings changes (rename, collaborators, branch protection, description) need an org owner until someone bumps him to admin on it. Repo description + LICENSE choice (open since 2026-08-02) can now be set org-side.

## 2026-08-04 — Deployment packaging: Docker image, CI publish to GHCR, k8s ops handoff

**Trigger.** DevOps (Darrell Starnes): apps deploy on k8s, from a Docker image, with the code in an official GitHub account — no individual servers. The repo transfer (entry above) satisfied the code-location half; this session delivers the image and the ops handoff. Separately, Albert completed the nvnmcite.com registrar transfer into Inveniam's GoDaddy account (2026-08-04), so the domain side is done and DNS is manageable by the team.

**Shipped.**
- `Dockerfile`, multi-stage. Build stage: `python:3.11-slim-bookworm` (3.11 = the interpreter the suite runs on locally) with the uv binary copied from `ghcr.io/astral-sh/uv:0.11` (minor-pinned; dev machine runs uv 0.11.16), `uv sync --frozen --no-dev` split into a dependency layer and a `--no-editable` project install so `/app/.venv` is self-contained (registry manifests, vendored ABI, webapp static ride along as package data). Amended same-session after CI run 1: the build stage installs `g++` — `fast-diff-match-patch` 2.1.0 (via eyecite) publishes no aarch64 wheel, so the arm64 leg compiles its C++ extension (build stage only; the runtime stage is untouched). Runtime stage: same base, non-root uid 10001, `EXPOSE 8787`, CMD `python -m nvnm_cite.webapp --host 0.0.0.0 --port 8787 --data-dir /app/data`. `/app/data` ships empty (the no-local-index path is the production shape); **no `--telemetry` flag — telemetry-off is baked into the image, enabling it would be a deliberate args override**. No secrets in the image; reads default to mainnet, `NVNM_NETWORK=testnet` available for a staging instance.
- `.dockerignore` as an ALLOWLIST (`pyproject.toml`, `uv.lock`, `README.md`, `src/` only): `.env`, local databases, and everything else structurally cannot enter the build context.
- `.github/workflows/ci.yml`: test job (astral-sh/setup-uv@v5, `uv sync --frozen --python 3.11`, full pytest) gating an image job: amd64 build → LIVE smoke test (run the container, `/` must serve, `/api/status` must report `rpc_ok` + `chain_id_ok` against mainnet 1611, `coverage.count ≥ 2114`, us-scotus sentinel `name_matches_manifest`, `telemetry.enabled == false`) → multi-arch amd64+arm64 push to **`ghcr.io/nvnm-chain/nvnm-cite`** (`latest` + `sha-*` on main pushes, semver on `v*` tags, PRs build without pushing). Auth is the workflow's own GITHUB_TOKEN; no new repo secrets. The smoke test intentionally depends on the public mainnet RPC being reachable from CI — a red build on RPC outage is honest signal, matching the app's fail-loud posture.
- `webapp/__main__.py`: SIGTERM now routes onto the existing KeyboardInterrupt clean-shutdown path (container runtimes stop with SIGTERM; previously that hard-killed mid-request). Verified live: `kill -TERM` → "bye", clean exit, port freed.
- `docs/DEPLOYMENT.md` (ops handoff) + a README Deployment section. Key semantics recorded for monitoring: **`/api/status` returns HTTP 200 whenever the process is healthy; RPC health is in-band** (`.chain.rpc_ok`, `.chain.chain_id_ok`, sentinel `name_matches_manifest` = the live manifest-integrity check) — so k8s probes assert HTTP 200 and the RPC alert parses the body; readiness must NOT key on `rpc_ok` (user-facing chain routes already fail loudly with 502, and unrouting pods can't fix an upstream RPC). Ingress requirements: body ≥ 30 MB (app enforces 30 MB/413 itself), read timeout ≥ 60 s, pass `Cache-Control: no-store` unchanged (no proxy cache/CDN), query strings out of retained access logs (receipt lookups carry the document SHA-256 in the query; the app's own log already strips them), root mount, TLS at the ingress, no CORS.
- New build/CI-time dependencies (zero runtime deps added), per the dependency rule: base image `python:3.11-slim-bookworm` (Docker official, PSFL); `ghcr.io/astral-sh/uv:0.11` (Apache-2.0/MIT); GitHub Actions `actions/checkout@v4`, `astral-sh/setup-uv@v5`, `docker/setup-qemu-action@v3`, `docker/setup-buildx-action@v3`, `docker/metadata-action@v5`, `docker/login-action@v3`, `docker/build-push-action@v6` (MIT/Apache-2.0).

**Verified.** 562 passed / 5 skipped after the SIGTERM change. The exact CI smoke assertions ran green against a local boot with an EMPTY data dir on live mainnet (chain 1611, head block 2176123, coverage 2114, sentinel manifest-consistent, telemetry off). No local Docker on the dev Mac — the container build itself is proven by the CI image job, which is the authoritative builder anyway.

**Open (not code).** (1) First push creates the GHCR package org-visible; an org admin flips `ghcr.io/nvnm-chain/nvnm-cite` public or grants devops pull access (Albert is write-not-admin). (2) The devops k8s stand-up + DNS cutover proceeds via their ticket flow (draft handed to Albert in-session). (3) Staging-first vs straight-to-domain stays Darrell's call — the image is identical either way.
